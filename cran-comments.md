## R CMD check results

0 errors | 0 warnings | 2 notes

* This is the first CRAN submission of `survalis` version 0.7.0.

* NOTE: `Imports includes 34 non-default packages.`
  `survalis` exports a unified learner interface and the goal of this release is
  that all documented learners work immediately after installation, so the
  runtime learner backends and CV/tuning dependencies are declared in `Imports`
  rather than left optional.

* NOTE: `unable to verify current time`
  This was produced by the local check environment and is not caused by package
  code.

## Reverse dependencies
- None (new package).
