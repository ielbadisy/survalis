% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rpart.R
\name{predict_rpart}
\alias{predict_rpart}
\title{Predict Survival Probabilities from an \code{rpart} Survival Tree}
\usage{
predict_rpart(object, newdata, times, ...)
}
\arguments{
\item{object}{An \code{"mlsurv_model"} object created by \code{\link[=fit_rpart]{fit_rpart()}}.}

\item{newdata}{A \code{data.frame} containing the predictor variables for prediction.}

\item{times}{Numeric vector of time points at which to compute survival probabilities.}

\item{...}{Additional arguments passed to internal prediction functions.}
}
\value{
A \code{data.frame} with one row per observation in \code{newdata} and one column
per requested time point, containing predicted survival probabilities.
}
\description{
Predicts survival probabilities at specified time points from a fitted
\code{rpart} survival tree model, assuming an exponential survival distribution
within each terminal node.
}
\details{
Predictions are based on converting the predicted mean survival times from the
survival tree into survival probabilities under an exponential assumption:
\deqn{S(t) = \exp(-t / \hat{\mu})}
where \eqn{\hat{\mu}} is the predicted mean survival time from the terminal node.
}
\examples{

mod_rpart <- fit_rpart(Surv(time, status) ~ age + karno + celltype, data = veteran)
predict_rpart(mod_rpart, newdata = veteran[1:5, ], times = c(100, 200, 300))

}
