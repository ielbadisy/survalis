% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_glmnet.R
\name{predict_glmnet}
\alias{predict_glmnet}
\title{Predict Survival Probabilities from a Penalized Cox Model (glmnet)}
\usage{
predict_glmnet(object, newdata, times, ...)
}
\arguments{
\item{object}{A fitted \code{"mlsurv_model"} object from \code{\link{fit_glmnet}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction.}

\item{times}{Numeric vector of time points at which to estimate survival
probabilities. Must be in the same scale as the training time variable.}

\item{...}{Not used.}
}
\value{
A \code{data.frame} with one row per observation in \code{newdata}
and one column per requested time point (\code{"t=<time>"}).
}
\description{
Predicts survival probabilities at specified time points from a fitted
penalized Cox proportional hazards model produced by \code{\link{fit_glmnet}}.
}
\details{
Predictions are computed by:
\enumerate{
\item Computing the linear predictors for training and new data at
\code{s = "lambda.min"}.
\item Fitting a Cox PH model on the training linear predictors to
estimate the baseline cumulative hazard.
\item Interpolating the baseline hazard at each \code{times} and
transforming via \eqn{S(t \mid x) = \exp(-H_0(t) \exp(\eta))}.
}
}
\examples{

mod_glmnet <- fit_glmnet(
  Surv(time, status) ~ age + karno + celltype,
  data = veteran
)

predict_glmnet(
  mod_glmnet,
  newdata = veteran[1:5, ],
  times = c(100, 200, 300)
)

}
\seealso{
\code{\link{fit_glmnet}}
}
