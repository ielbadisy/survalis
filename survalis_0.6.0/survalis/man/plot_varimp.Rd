% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_varimp.R
\name{plot_varimp}
\alias{plot_varimp}
\title{Plot Permutation Variable Importance}
\usage{
plot_varimp(varimp_df, use_scaled = TRUE)
}
\arguments{
\item{varimp_df}{A data frame as returned by \code{\link{compute_varimp}}.}

\item{use_scaled}{Logical; if \code{TRUE} (default), plot \code{scaled_importance}
(percent). If unavailable, falls back to raw \code{importance} with a warning.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Creates a dot plot of permutation-based variable importance, using either the
scaled importance (default) or the raw importance column.
}
\examples{
# p1 <- plot_varimp(imp, use_scaled = TRUE)
# p2 <- plot_varimp(imp, use_scaled = FALSE)
}
