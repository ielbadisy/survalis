% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_selectcox.R
\name{tune_selectcox}
\alias{tune_selectcox}
\title{Tune SelectCox Rule (Cross-Validation)}
\usage{
tune_selectcox(
  formula,
  data,
  times,
  rules = c("aic", "p"),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  refit_best = TRUE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.
The left‑hand side must be a \code{Surv()} object from the \pkg{survival} package.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{times}{Numeric vector of evaluation time points (same scale as the
training survival time). Must be non‑negative and finite.}

\item{rules}{Character vector of selection rules to compare (e.g.,
\code{c("aic","p")}).}

\item{metrics}{Character vector of metrics to evaluate/optimize
(e.g., \code{"cindex"}, \code{"ibs"}, \code{"ise"}). The first entry is the primary
selection metric.}

\item{folds}{Number of cross‑validation folds.}

\item{seed}{Integer random seed for reproducibility.}

\item{refit_best}{Logical; if \code{TRUE}, refits the best rule on the full data
and returns it as an \code{mlsurv_model} (with tuning results attached).}

\item{...}{Additional arguments forwarded to the underlying engine where applicable.}
}
\value{
If \code{refit_best = FALSE}, a \code{data.frame} (class \code{"tuned_surv"}) with a
row per rule and metric columns, ordered by the first metric. If
\code{refit_best = TRUE}, a fitted \code{mlsurv_model} from \code{\link[=fit_selectcox]{fit_selectcox()}} with
attribute \code{"tuning_results"} containing the full results.
}
\description{
Cross‑validates \pkg{pec}'s \code{selectCox()} across one or more selection
rules and selects the best configuration by the primary metric. Optionally
refits the best rule on the full dataset.
}
\details{
\strong{Evaluation.} Internally calls \code{cv_survlearner()} with
\code{fit_selectcox()}/\code{predict_selectcox()} to ensure tuning uses the same code
paths as production. Rules typically include \code{"aic"} and/or \code{"p"}.
}
\examples{
res_selectcox <- tune_selectcox(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  rules = c("aic", "p"),
  metrics = c("cindex", "ibs", "ise"),
  folds = 3,
  refit_best = FALSE
)

print(res_selectcox)
class(res_selectcox)

mod_selectcox <- tune_selectcox(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  rules = c("aic", "p"),
  metrics = c("cindex", "ibs", "ise"),
  folds = 3,
  refit_best = TRUE
)

summary(mod_selectcox)

}
\seealso{
\code{\link[=fit_selectcox]{fit_selectcox()}}, \code{\link[=predict_selectcox]{predict_selectcox()}}, \code{pec::selectCox()}
}
