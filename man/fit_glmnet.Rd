% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_glmnet.R
\name{fit_glmnet}
\alias{fit_glmnet}
\title{Fit a Penalized Cox Proportional Hazards Model (glmnet)}
\usage{
fit_glmnet(formula, data, alpha = 1, ...)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{alpha}{Numeric value in \code{[0, 1]} specifying the elastic net
mixing parameter:
\itemize{
\item \code{1} for LASSO penalty
\item \code{0} for ridge penalty
\item between 0 and 1 for elastic net
}}

\item{...}{Additional arguments passed to \code{\link[glmnet]{cv.glmnet}}.}
}
\value{
An object of class \code{"mlsurv_model"} containing:
\itemize{
\item \code{model} -- the fitted \code{cv.glmnet} object
\item \code{learner} -- the string \code{"glmnet"}
\item \code{formula}, \code{data}, \code{time}, and \code{status} metadata
}
}
\description{
Fits a Cox proportional hazards model with elastic net regularization
using \pkg{glmnet}. The function wraps \code{\link[glmnet]{cv.glmnet}} to
select the optimal penalty parameter \eqn{\lambda} by cross-validation.
}
\examples{

mod_glmnet <- fit_glmnet(
  Surv(time, status) ~ age + karno + celltype,
  data = veteran
)

summary(mod_glmnet$model)

}
\seealso{
\code{\link[glmnet]{cv.glmnet}}, \code{\link{predict_glmnet}}
}
