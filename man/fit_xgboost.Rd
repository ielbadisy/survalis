% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_xgboost.R
\name{fit_xgboost}
\alias{fit_xgboost}
\title{Fit an XGBoost Survival Model (mlsurv_model-compatible)}
\usage{
fit_xgboost(
  formula,
  data,
  booster = "gbtree",
  objective = "survival:aft",
  aft_loss_distribution = "extreme",
  aft_loss_distribution_scale = 1,
  nrounds = 100
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing variables in \code{formula}.}

\item{booster}{XGBoost booster type (default \code{"gbtree"}).}

\item{objective}{One of \code{"survival:aft"} (default) or \code{"survival:cox"}.}

\item{aft_loss_distribution}{AFT error distribution: \code{"extreme"} (default),
\code{"logistic"}, or \code{"normal"}.}

\item{aft_loss_distribution_scale}{Positive numeric scale for the AFT loss (default \code{1}).}

\item{nrounds}{Integer number of boosting iterations (default \code{100}).}
}
\value{
An object of class \code{mlsurv_model}:
\describe{
\item{model}{Fitted \code{xgboost} model.}
\item{learner}{\code{"xgboost"}.}
\item{formula, data}{Original inputs.}
\item{time, status}{Names of the survival time and event variables.}
\item{objective}{Objective used.}
\item{dist, scale}{AFT distribution and scale (populated even if Cox is used).}
\item{terms}{Preserved \code{terms} for consistent prediction design matrices.}
}
}
\description{
Fits a gradient-boosted tree model for time-to-event outcomes using
\pkg{xgboost}. Supports \code{"survival:aft"} (default) and \code{"survival:cox"}.
Returns an \code{mlsurv_model} object compatible with the \code{survalis} pipeline.
}
\details{
Design contract: returns a named list with engine metadata and preserves
\code{terms(formula)} so prediction uses the exact same encoding as training.
}
\section{Engine}{

Uses \strong{xgboost}. For \code{"survival:aft"}, interval labels are set via
\code{label_lower_bound}/\code{label_upper_bound} with right-censoring
represented by \code{Inf} on the upper bound. For \code{"survival:cox"}, labels
follow the Cox objective convention.
}

\examples{
\donttest{
  mod_xgb <- fit_xgboost(
    Surv(time, status) ~ age + karno + celltype,
    data = veteran
  )
  }

}
\seealso{
\code{\link[=predict_xgboost]{predict_xgboost()}}, \code{\link[=tune_xgboost]{tune_xgboost()}}
}
