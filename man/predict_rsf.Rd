% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rsf.R
\name{predict_rsf}
\alias{predict_rsf}
\title{Predict Survival Probabilities from an RSF Model}
\usage{
predict_rsf(object, newdata, times = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{mlsurv_model} returned by \code{\link[=fit_rsf]{fit_rsf()}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction. Must include
the same predictor variables used at training time.}

\item{times}{Optional numeric vector of time points at which to return survival
probabilities. If \code{NULL}, predictions are returned at the model's internal
time grid.}

\item{...}{Additional arguments forwarded to
\code{\link[randomForestSRC]{predict.rfsrc}}.}
}
\value{
A base \code{data.frame} of survival probabilities with one row per
observation in \code{newdata} and columns named \code{t={time}} (character), containing
numeric values in [0, 1].
}
\description{
Generates survival probability predictions for new data using a model
fitted by \code{\link[=fit_rsf]{fit_rsf()}}.
}
\details{
If \code{times} is provided, the function aligns predictions by selecting, for each
requested time, the closest available time from the RSF prediction object's
\code{time.interest} grid (nearest-neighbor matching).
}
\examples{
\dontshow{if (requireNamespace("randomForestSRC", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod_rsf <- fit_rsf(Surv(time, status) ~ age + celltype + karno,
                   data = veteran, ntree = 200)

times <- c(100, 200, 300)
pred_probs <- predict_rsf(mod_rsf, newdata = veteran[1:5, ], times = times)
print(round(pred_probs, 3))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fit_rsf]{fit_rsf()}}, \code{\link[randomForestSRC]{predict.rfsrc}}
}
