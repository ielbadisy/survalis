% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_blackboost.R
\name{tune_blackboost}
\alias{tune_blackboost}
\title{Tune blackboost Hyperparameters (Cross-Validation)}
\usage{
tune_blackboost(
  formula,
  data,
  times,
  param_grid = expand.grid(mstop = c(50, 100, 200), nu = c(0.05, 0.1), maxdepth = c(2,
    3)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing variables referenced in \code{formula}.}

\item{times}{Numeric vector of evaluation time points (same scale as the training time).}

\item{param_grid}{A \code{data.frame} (e.g., from \code{expand.grid()}) with columns
such as \code{mstop}, \code{nu}, and \code{maxdepth}.}

\item{metrics}{Character vector of metrics to compute (e.g., \code{"cindex"}, \code{"ibs"}).
The first entry is the primary selection metric.}

\item{folds}{Integer; number of CV folds. Default \code{5}.}

\item{seed}{Integer random seed for reproducibility. Default \code{123}.}

\item{ncores}{Integer number of CPU cores passed to \code{\link{cv_survlearner}}
for fold evaluation (default \code{1}).}

\item{refit_best}{Logical; if \code{TRUE}, refits the best configuration on the full
data and returns it. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{\link[=fit_blackboost]{fit_blackboost()}}.}
}
\value{
If \code{refit_best = FALSE}, a \code{data.frame} (class \code{"tuned_surv"}) of grid
results with metric columns, sorted by the primary metric. If \code{refit_best = TRUE},
a fitted \code{mlsurv_model} from \code{\link[=fit_blackboost]{fit_blackboost()}} using the selected hyperparameters.
}
\description{
Cross-validates \pkg{mboost} Cox boosting models over a hyperparameter grid
and selects the best configuration according to the primary metric. Optionally
refits the best model on the full dataset.
}
\details{
Internally calls \code{cv_survlearner()} with \code{fit_blackboost()}/\code{predict_blackboost()}
so tuning mirrors the production path. Typical grids vary \code{mstop}, \code{nu}, and
tree \code{maxdepth}.
}
\examples{
\donttest{
# Grid search only
res_blackboost <- tune_blackboost(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(5, 10, 40),
  param_grid = expand.grid(
    mstop = c(100, 200),
    nu = c(0.05, 0.1),
    maxdepth = c(2, 3)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3
)
print(res_blackboost)

# Refit best model
mod_blackboost_best <- tune_blackboost(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(5, 10, 40),
  param_grid = expand.grid(
    mstop = c(100, 200),
    nu = c(0.05, 0.1),
    maxdepth = c(2, 3)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3,
  refit_best = TRUE
)
summary(mod_blackboost_best)
}
}
