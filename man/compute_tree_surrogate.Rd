% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_tree_surrogate.R
\name{compute_tree_surrogate}
\alias{compute_tree_surrogate}
\title{Compute Tree-Based Surrogate Model for Survival Predictions}
\usage{
compute_tree_surrogate(model, data, times, minsplit = 10, cp = 0.01)
}
\arguments{
\item{model}{A fitted survival model object created with a \verb{fit_*()} function.
Must include a valid \code{learner} field corresponding to a \verb{predict_*()} method.}

\item{data}{A data frame containing predictor variables (and optional survival outcome columns).}

\item{times}{A numeric vector of evaluation times at which to approximate model predictions.
Must contain at least one value.}

\item{minsplit}{Minimum number of observations required to attempt a split in the surrogate tree.
Passed to \code{rpart::rpart.control()}.}

\item{cp}{Complexity parameter for the surrogate tree. Passed to \code{rpart::rpart.control()}.}
}
\value{
An object of class \code{"tree_surrogate"}, containing:
\itemize{
\item \code{times}: the evaluation times.
\item \code{results}: a list with one element per time, each containing:
\itemize{
\item \code{tree}: the fitted \code{rpart} object.
\item \code{r_squared}: the R of the surrogate model vs. the original predictions.
\item \code{split_count}: a table of feature split counts.
}
\item \code{dynamic}: logical indicating if more than one time was used.
}
}
\description{
Fits decision tree surrogate models to approximate the predictions of a fitted survival model
at one or more evaluation times. This allows users to gain interpretable, rule-based
approximations of complex survival models.
}
\details{
For each evaluation time, the function:
\enumerate{
\item Predicts survival probabilities from the fitted model.
\item Excludes survival outcome columns (\code{time}, \code{status}, \code{event}) from the predictors.
\item Fits a decision tree to approximate the predicted probabilities.
\item Computes the R between the model predictions and the surrogate predictions.
\item Counts the number of splits per feature.
}

If multiple \code{times} are provided, results are stored for each time point.
}
\examples{
\donttest{
mod_ranger <- fit_ranger(Surv(time, status) ~ age + karno + celltype, data = veteran)
tree_ranger <- compute_tree_surrogate(
  model = mod_ranger,
  data = veteran,
  times = c(100, 200, 300)
)
}

}
