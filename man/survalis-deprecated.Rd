% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survalis-deprecated.R
\name{survalis-deprecated}
\alias{survalis-deprecated}
\title{Superseded functions in survalis 2.x}
\description{
survalis 2.0 introduced the seven-verb interface -- \code{\link[=fit]{fit()}}, \code{\link[=predict]{predict()}},
\code{\link[=evaluate]{evaluate()}}, \code{\link[=tune]{tune()}}, \code{\link[=compare]{compare()}}, \code{\link[=interpret]{interpret()}}, \code{\link[=estimate]{estimate()}} -- plus the
resampling constructors (\code{\link[=cv]{cv()}}, \code{\link[=holdout]{holdout()}}, \code{\link[=group_cv]{group_cv()}}, \code{\link[=bootstrap]{bootstrap()}}).
}
\details{
The granular functions listed below are the machinery those verbs are built
on. They remain \strong{exported and fully supported for the whole 2.x series}, are
marked internal in the manual, and emit a one-time note per session. They are
scheduled to become internal (unexported) in survalis 3.0.
}
\section{Mapping}{

\tabular{ll}{
\strong{Superseded} \tab \strong{Use instead} \cr
\verb{fit_<learner>()} \tab \code{fit(formula, data, model = "<learner>")} \cr
\verb{predict_<learner>()} \tab \code{predict(fit, newdata, times, type)} \cr
\verb{tune_<learner>()} \tab \code{tune(formula, data, model = "<learner>", grid)} \cr
\code{cv_survlearner()}, \code{cv_summary()}, \code{cv_plot()} \tab \code{evaluate(...)} \cr
\code{score_survmodel()} \tab \code{evaluate(fit, resampling = ...)} \cr
\code{benchmark()}, \code{benchmark_default_survlearners()},
\code{benchmark_tuned_survlearners()} \tab \code{compare(...)} \cr
\code{summarise_benchmark()}, \code{summarize_benchmark_results()},
\code{best_survlearner()} \tab \code{summary()} / \code{print()} on a \code{survalis_compare} \cr
\verb{compute_*()} / \verb{plot_*()} \tab \code{interpret(fit, method = ...)} then \code{plot()} \cr
\code{estimate_rmst()} \tab \code{estimate(..., estimand = "RMST_diff")} \cr
}
}

\keyword{internal}
