% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_densemlp.R
\name{fit_densemlp}
\alias{fit_densemlp}
\title{Fit a densemlp Survival Model}
\usage{
fit_densemlp(
  formula,
  data,
  loss = c("brier", "cox"),
  n_bins = 10,
  hidden_units = c(32, 16),
  epochs = 100,
  lr = 0.001,
  ...
)
}
\arguments{
\item{formula}{A survival formula \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the formula variables.}

\item{loss}{Survival loss: \code{"brier"} (default, discrete-time hazard grid,
required for survival-curve prediction) or \code{"cox"} (a single linear
risk score only).}

\item{n_bins}{Number of discrete-time bins for \code{loss = "brier"}
(default \code{10}).}

\item{hidden_units}{Integer vector of hidden-layer widths (default
\code{c(32, 16)}).}

\item{epochs}{Maximum training epochs (default \code{100}).}

\item{lr}{Adam learning rate (default \code{1e-3}).}

\item{...}{Additional arguments passed to \code{densemlp::densemlp()} (for
example \code{batch_size}, \code{dropout}, \code{batch_norm},
\code{early_stopping}, \code{patience}, \code{ensemble}, \code{seed},
\code{ncores}).}
}
\value{
An object of class \code{"mlsurv_model"} with elements \code{model},
\code{learner} (\code{"densemlp"}), \code{formula}, \code{data},
\code{time}, \code{status}, and \code{loss}.
}
\description{
Fits a compact feedforward neural network for right-censored survival data
with the \pkg{densemlp} package (native C++ backend, no \pkg{torch}
dependency) and returns an object compatible with the \code{mlsurv_model}
interface. The IPCW integrated-Brier loss (\code{loss = "brier"}) is used by
default so that a full survival-probability curve is available; predictions
are interpolated from the network's discrete-time grid onto the requested
evaluation times.
}
\examples{
\dontshow{if (requireNamespace("densemlp", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_densemlp(Surv(time, status) ~ age + karno + celltype, veteran,
                    n_bins = 8, epochs = 20, hidden_units = c(16, 8))
summary(mod)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{predict_densemlp}}, \code{\link{tune_densemlp}},
\code{\link[densemlp]{densemlp}}
}
