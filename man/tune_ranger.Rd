% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ranger.R
\name{tune_ranger}
\alias{tune_ranger}
\title{Hyperparameter Tuning for ranger Survival Models}
\usage{
tune_ranger(
  formula,
  data,
  times,
  param_grid = expand.grid(num.trees = c(100, 300), mtry = c(1, 2, 3), min.node.size =
    c(3, 5)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the variables in the model.}

\item{times}{Numeric vector of time points at which to evaluate performance.}

\item{param_grid}{A \code{data.frame} of hyperparameter combinations. Must contain
columns \code{num.trees}, \code{mtry}, and \code{min.node.size}.}

\item{metrics}{Character vector of evaluation metrics (e.g., \code{"cindex"}, \code{"ibs"}).}

\item{folds}{Number of cross-validation folds.}

\item{seed}{Random seed for reproducibility.}

\item{ncores}{Integer number of CPU cores passed to \code{\link{cv_survlearner}}
for fold evaluation (default \code{1}).}

\item{refit_best}{Logical; if \code{TRUE}, refits the model using the best parameters.}

\item{...}{Additional arguments passed to \code{\link{fit_ranger}}.}
}
\value{
If \code{refit_best = FALSE}, returns a \code{data.frame} of tuning results
sorted by the primary metric. If \code{refit_best = TRUE}, returns an
\code{"mlsurv_model"} object with the best parameters and an attribute
\code{"tuning_results"}.
}
\description{
Performs grid search tuning of a survival random forest model using
\pkg{ranger} over a set of hyperparameter combinations.
}
\details{
Uses \code{\link{cv_survlearner}} to perform cross-validation for each
parameter combination. If \code{refit_best = TRUE}, the function returns the
best-fitting model; otherwise, it returns a tuning results table.
}
\examples{
\dontshow{if (requireNamespace("ranger", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod_ranger_best <- tune_ranger(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = expand.grid(
    num.trees = c(100, 300),
    mtry = c(1, 2),
    min.node.size = c(3, 5)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3,
  refit_best = TRUE
)
summary(mod_ranger_best)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{fit_ranger}}, \code{\link{predict_ranger}}
}
