% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_coxboost.R
\name{predict_coxboost}
\alias{predict_coxboost}
\title{Predict Survival with CoxBoost}
\usage{
predict_coxboost(object, newdata, times = NULL)
}
\arguments{
\item{object}{A fitted \code{mlsurv_model} returned by \code{\link[=fit_coxboost]{fit_coxboost()}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction.}

\item{times}{Optional numeric vector of evaluation time points. If
\code{NULL}, returns the linear predictor.}
}
\value{
\itemize{
\item If \code{times} is \code{NULL}: a numeric vector of linear predictors
(one per row of \code{newdata}).
\item If \code{times} is provided: a base \code{data.frame} of survival
probabilities with columns named \code{"t=<time>"}.
}
}
\description{
Generates survival-probability predictions from a CoxBoost
\code{mlsurv_model}.
}
\examples{
\donttest{
  mod_cb <- fit_coxboost(Surv(time, status) ~ age + karno + celltype,
                         data = veteran, stepno = 30)
  predict_coxboost(mod_cb, newdata = veteran[1:5, ], times = c(100, 200, 300))
}

}
\seealso{
\code{\link[=fit_coxboost]{fit_coxboost()}}, \code{\link[=tune_coxboost]{tune_coxboost()}}
}
