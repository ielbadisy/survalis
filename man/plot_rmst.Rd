% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_rmst.R
\name{plot_rmst}
\alias{plot_rmst}
\title{RMST Estimate or Contrast Curve Over a Grid of Restriction Times}
\usage{
plot_rmst(
  model,
  data,
  tau_seq,
  times,
  trt_col = NULL,
  R = 100,
  conf = 0.95,
  seed = NULL,
  title
)
}
\arguments{
\item{model}{An \code{mlsurv_model} object from a \code{fit_*()} learner.}

\item{data}{A data.frame of covariates.}

\item{tau_seq}{Numeric vector of restriction times to evaluate.}

\item{times}{Numeric vector of evaluation times passed to
\code{predict_<learner>()}.}

\item{trt_col}{Optional character; name of a binary grouping/exposure
column. \code{NULL} (default) plots the marginal RMST(tau) curve;
supplying it plots the RMST contrast curve.}

\item{R}{Number of bootstrap replicates per \code{tau}. Default \code{100}.}

\item{conf}{Confidence level in (0, 1). Default \code{0.95}.}

\item{seed}{Optional RNG seed.}

\item{title}{Plot title. If missing, an automatically generated title is
used. Pass \code{NULL} to omit the title entirely.}
}
\value{
A \pkg{ggplot2} object with an attribute \code{"curve"} holding the
underlying \code{data.frame} (\code{tau}, \code{estimate}, \code{lo},
\code{hi}).
}
\description{
Evaluates \code{\link{estimate_rmst}} across a grid of restriction times
\eqn{\tau} and plots the resulting curve with percentile bootstrap
confidence bands. As in \code{\link{estimate_rmst}}, marginal vs. contrast
mode is dispatched by \code{trt_col} rather than by separate function names.
}
\examples{
\donttest{
mod <- fit_coxph(Surv(time, status) ~ age + karno + trt, data = veteran)
times <- seq(10, 300, by = 10)
plot_rmst(mod, data = veteran, tau_seq = seq(50, 250, by = 50),
                times = times, trt_col = "trt", R = 10, seed = 1)
}
}
\seealso{
\code{\link[=estimate_rmst]{estimate_rmst()}}
}
