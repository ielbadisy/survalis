% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-helpers.R
\name{.survmat_as_matrix}
\alias{.survmat_as_matrix}
\title{Validate and standardize a survival-probability matrix (survmat)}
\usage{
.survmat_as_matrix(S, times = NULL, strict_colnames = FALSE, clamp = FALSE)
}
\arguments{
\item{S}{A \code{surv_mat}: numeric matrix or data.frame of survival probabilities.}

\item{times}{Optional numeric vector of time points. If provided, requires
\code{length(times) == ncol(S)} and enforces the column names \code{"t=<time>"}.}

\item{strict_colnames}{Logical; if \code{TRUE} and \code{times} is provided, checks that
existing colnames are exactly \code{"t=<time>"} (after coercion to character).
If \code{FALSE}, colnames are overwritten to match the contract.}

\item{clamp}{Logical; if \code{TRUE}, clamps \code{S} into \verb{[0,1]}.}
}
\value{
A numeric matrix.
}
\description{
Internal utility to validate the basic structure of a \code{surv_mat} and coerce it
to a numeric matrix. Optionally checks column names against \code{times}.
}
\keyword{internal}
