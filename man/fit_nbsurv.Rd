% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_nbsurv.R
\name{fit_nbsurv}
\alias{fit_nbsurv}
\title{Fit a Conditional Naive Bayes Survival Model}
\usage{
fit_nbsurv(formula, data, scale = TRUE, laplace = 1, min_sd = 0.05, ...)
}
\arguments{
\item{formula}{A survival formula \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the formula variables.}

\item{scale}{Logical; standardize continuous predictors using training
mean/sd (default \code{TRUE}).}

\item{laplace}{Laplace smoothing constant for categorical predictors
(default \code{1}).}

\item{min_sd}{Floor for estimated SDs of Gaussian class-conditional
distributions (default \code{0.05}).}

\item{...}{Additional arguments passed to \code{nbsurv::nbsurv()} (for
example \code{time_grid}, \code{cov_structure}, \code{shrinkage},
\code{time_smooth}, \code{bandwidth}).}
}
\value{
An object of class \code{"mlsurv_model"} with elements \code{model},
\code{learner} (\code{"nbsurv"}), \code{formula}, \code{data}, \code{time},
and \code{status}.
}
\description{
Fits a conditional naive Bayes survival model with the \pkg{nbsurv}
package and returns an object compatible with the \code{mlsurv_model}
interface. Frames survival at each horizon as a binary classification
problem ("survive beyond t" vs. "fail before t") and applies Bayes' rule
under a conditional-independence assumption over predictors, with IPCW
weighting for censoring.
}
\examples{
\dontshow{if (requireNamespace("nbsurv", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_nbsurv(Surv(time, status) ~ age + karno + celltype, veteran)
summary(mod)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{predict_nbsurv}}, \code{\link[nbsurv]{nbsurv}},
\code{\link{fit}} for the verb interface
}
\keyword{internal}
