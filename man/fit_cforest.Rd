% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cforest.R
\name{fit_cforest}
\alias{fit_cforest}
\title{Fit a Conditional Inference Survival Forest}
\usage{
fit_cforest(
  formula,
  data,
  teststat = "quad",
  testtype = "Univariate",
  mincriterion = 0,
  ntree = 500,
  mtry = 5,
  replace = TRUE,
  fraction = 0.632,
  ...
)
}
\arguments{
\item{formula}{A \code{Surv()} survival formula specifying the outcome and predictors.}

\item{data}{A data frame containing the variables in the model.}

\item{teststat}{Character string specifying the test statistic to use
(default = \code{"quad"}). See \code{\link[party:cforest_control]{party::cforest_control()}}.}

\item{testtype}{Character string specifying the type of test
(default = \code{"Univariate"}).}

\item{mincriterion}{Numeric, the value of the test statistic that must be
exceeded for a split to be performed (default = \code{0}).}

\item{ntree}{Integer, number of trees to grow (default = \code{500}).}

\item{mtry}{Integer, number of variables randomly selected at each node
(default = \code{5}).}

\item{replace}{Logical, whether sampling of cases is with replacement
(default = \code{TRUE}).}

\item{fraction}{Proportion of samples to draw if \code{replace = FALSE}
(default = \code{0.632}).}

\item{...}{Additional arguments passed to \code{\link[party:cforest_control]{party::cforest_control()}}.}
}
\value{
An object of class \code{"mlsurv_model"}, containing:
\item{model}{The fitted \code{cforest} model.}
\item{learner}{Character string \code{"cforest"}.}
\item{formula}{The model formula.}
\item{data}{The training data.}
}
\description{
Fits a survival forest model using the \code{party::cforest()} implementation
of conditional inference trees for right-censored survival data.
}
\details{
This function wraps \code{party::cforest()} to fit a conditional inference
forest for survival analysis, returning an \code{mlsurv_model} object compatible
with \code{predict_cforest()} and the \code{survalis} framework.
}
\examples{
\dontshow{if (requireNamespace("party", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_cforest(Surv(time, status) ~ age + celltype + karno, data = veteran)
head(predict_cforest(mod, newdata = veteran[1:5, ], times = c(100, 200)))
\dontshow{\}) # examplesIf}
}
