% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rpsurv.R
\name{fit_rpsurv}
\alias{fit_rpsurv}
\title{Fit a Royston-Parmar Flexible Parametric Survival Model}
\usage{
fit_rpsurv(
  formula,
  data,
  df = 4,
  knots = NULL,
  tve = NULL,
  tve.df = 3,
  scale = c("hazard", "odds", "normal"),
  ...
)
}
\arguments{
\item{formula}{A survival formula \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the formula variables.}

\item{df}{Degrees of freedom for the baseline spline (default \code{4}).}

\item{knots}{Optional explicit knot vector (log time scale); default
\code{NULL} derives knots from \code{df}.}

\item{tve}{Optional character vector of covariate names given
time-varying effects via their own restricted cubic spline.}

\item{tve.df}{Degrees of freedom for each time-varying-effect spline
(default \code{3}).}

\item{scale}{One of \code{"hazard"} (proportional hazards, default),
\code{"odds"}, or \code{"normal"}.}

\item{...}{Additional arguments passed to \code{rpsurv::rpsurv()} (for
example \code{control}).}
}
\value{
An object of class \code{"mlsurv_model"} with elements \code{model},
\code{learner} (\code{"rpsurv"}), \code{formula}, \code{data}, \code{time},
\code{status}, plus \code{rhs_terms}/\code{cov_terms}/\code{xlev} used to
rebuild the design matrix for arbitrary \code{newdata} at predict time.
}
\description{
Fits a Royston-Parmar flexible parametric survival model with the
\pkg{rpsurv} package and returns an object compatible with the
\code{mlsurv_model} interface. Restricted cubic splines model the baseline on
the chosen \code{scale}, optionally with time-varying covariate effects
(\code{tve}), so predicted survival curves are available at arbitrary
time points without a proportional-hazards assumption.
}
\examples{
\dontshow{if (requireNamespace("rpsurv", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_rpsurv(Surv(time, status) ~ age + karno + celltype, veteran, df = 3)
summary(mod)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{predict_rpsurv}}, \code{\link[rpsurv]{rpsurv}},
\code{\link{fit}} for the verb interface
}
\keyword{internal}
