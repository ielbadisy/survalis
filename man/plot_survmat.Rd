% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-helpers.R
\name{plot_survmat}
\alias{plot_survmat}
\title{Plot Predicted Survival Curves from a survmat}
\usage{
plot_survmat(
  S,
  times = NULL,
  group = NULL,
  ids = NULL,
  summary_fun = c("mean", "median"),
  show_individual = NULL,
  alpha = 0.2,
  linewidth = 0.7
)
}
\arguments{
\item{S}{A \code{surv_mat}: numeric matrix/data.frame of survival probabilities.}

\item{times}{Optional numeric vector of time points corresponding to columns
of \code{S}. If omitted, times are inferred from column names of the form
\code{"t=<time>"}.}

\item{group}{Optional vector of group labels of length \code{nrow(S)}. When
supplied, grouped summary curves are plotted.}

\item{ids}{Optional integer vector of row ids to subset before plotting.}

\item{summary_fun}{Aggregation for grouped curves; one of \code{"mean"}
(default) or \code{"median"}.}

\item{show_individual}{Logical. If \code{NULL} (default), individual curves
are shown only when \code{group} is omitted. If \code{TRUE}, individual
curves are overlaid beneath the grouped summary curves.}

\item{alpha}{Alpha transparency for individual curves (default \code{0.2}).}

\item{linewidth}{Line width for plotted curves (default \code{0.7}).}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Plots one or more predicted survival curves from a survival-probability
matrix. By default, each row of \code{S} is shown as an individual curve. If
a \code{group} vector is supplied, curves are summarized by group using the
requested aggregation function.
}
\examples{
S <- data.frame(`t=1` = c(0.95, 0.90, 0.92),
                `t=2` = c(0.80, 0.70, 0.78),
                `t=3` = c(0.60, 0.45, 0.55))
plot_survmat(S)
plot_survmat(S, group = c("A", "B", "A"))

}
