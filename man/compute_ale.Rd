% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ale.R
\name{compute_ale}
\alias{compute_ale}
\title{Accumulated Local Effects (ALE) for Survival Models}
\usage{
compute_ale(model, newdata, feature, times, grid.size = 20)
}
\arguments{
\item{model}{An \code{mlsurv_model} created by a \verb{fit_*()} function. Must include
a valid \code{learner} so that \verb{predict_<learner>()} can be dispatched.}

\item{newdata}{Data frame used to compute ALE (typically the training set or
a representative sample).}

\item{feature}{Single \strong{numeric/continuous} feature name for which to
compute ALE. Categorical features are not supported here (use PDP/ICE).}

\item{times}{Numeric vector of time points at which to evaluate survival
probabilities.}

\item{grid.size}{Integer number of quantile cut points used to build the ALE
grid (default 20). The algorithm uses quantiles of \code{newdata[[feature]]}.}
}
\value{
A list with:
\describe{
\item{ale}{A data frame with columns \code{feature_value} and one column per
time (\code{"t=<time>"}) containing centered ALE effects.}
\item{integrated}{If multiple times were provided, a data frame with
columns \code{feature_value} and \code{integrated_ale} equal to the mean of per-time
ALE effects across \code{times}; otherwise \code{NULL}.}
}
}
\description{
Computes ALE curves for a numeric (continuous) feature with respect to
survival probabilities at one or more evaluation times. ALE summarizes the
average \emph{local} effect of changing a feature within small intervals, is
robust to correlated features, and is centered to have mean zero.
}
\details{
For consecutive quantile bins \eqn{[z_k, z_{k+1}]} of the target feature,
ALE integrates the \emph{local} change in the model prediction when moving the
feature from \eqn{z_k} to \eqn{z_{k+1}} while holding all other features at
their observed values, and then accumulates these differences across bins.
For survival models, predictions are survival probabilities at \code{times}.
The returned ALE curves are centered (mean zero across the grid) per time.
}
\examples{
# mod <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
# ale_res <- compute_ale(mod, newdata = veteran, feature = "karno",
#                        times = c(100, 200, 300))
# head(ale_res$ale)

}
\seealso{
\code{\link[=plot_ale]{plot_ale()}}, \code{\link[=compute_pdp]{compute_pdp()}}
}
