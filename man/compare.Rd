% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-compare.R
\name{compare}
\alias{compare}
\title{Compare several survival learners on shared resampling folds}
\usage{
compare(
  formula,
  data,
  models,
  specs = list(),
  times = NULL,
  resampling = cv(),
  metrics = c("cindex", "ibs"),
  tune = FALSE,
  ncores = 1,
  ...
)
}
\arguments{
\item{formula}{A survival formula.}

\item{data}{A data frame.}

\item{models}{Character vector of learner ids.}

\item{specs}{Named list of engine-argument lists, keyed by model id
(\code{specs[["ranger"]] <- list(num.trees = 500)}).}

\item{times}{Evaluation grid; \code{NULL} uses \code{\link[=default_times]{default_times()}}.}

\item{resampling}{A \link{resampling} spec (default \code{cv()}); the split set is drawn
once and reused for every model.}

\item{metrics}{Character vector from \code{list_metrics()$metric}.}

\item{tune}{Logical. When \code{TRUE}, each tunable model is tuned once on the full
data (its default grid) and the winning configuration is then evaluated on
the shared folds. This is not nested CV, so the tuned results are optimistic;
a message is emitted. Default \code{FALSE}.}

\item{ncores}{Cores for the split loop (default \code{1}).}

\item{...}{Unused.}
}
\value{
A \code{survalis_compare} object: \code{leaderboard} (model x metric, mean and
se), \code{per_split} (long), \code{times}, \code{resampling}, \code{metrics}, \code{models}, \code{tuned}.
}
\description{
Evaluates each learner in \code{models} on the \emph{same} set of resampling splits, so
the per-fold values are paired across models. Returns a leaderboard plus the
full per-split table.
}
\examples{
\dontshow{if (requireNamespace("ranger", quietly = TRUE)) withAutoprint(\{ # examplesIf}
cmp <- compare(Surv(time, status) ~ age + karno + celltype, veteran,
               models = c("coxph", "ranger"), times = c(90, 180, 365),
               resampling = cv(3))
cmp
plot(cmp)
\dontshow{\}) # examplesIf}
}
