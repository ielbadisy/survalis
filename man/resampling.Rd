% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resampling.R
\name{resampling}
\alias{resampling}
\alias{cv}
\alias{holdout}
\alias{group_cv}
\alias{bootstrap}
\title{Resampling specifications for evaluate(), tune() and compare()}
\usage{
cv(v = 5, repeats = 1, strata = "status", seed = NULL)

holdout(prop = 0.75, strata = "status", seed = NULL)

group_cv(group, v = 5, seed = NULL)

bootstrap(times = 25, seed = NULL)
}
\arguments{
\item{v}{Integer number of folds (default \code{5}).}

\item{repeats}{Integer number of times the whole v-fold scheme is repeated
with fresh random folds (default \code{1}).}

\item{strata}{Column name to stratify on, or \code{NA} to disable stratification.
The default sentinel \code{"status"} means "use the event-indicator column of the
model formula", resolved by the caller.}

\item{seed}{Optional integer seed. Set it (and keep it equal across models)
for paired resampling.}

\item{prop}{Proportion of rows assigned to the training set in \code{holdout()}
(default \code{0.75}).}

\item{group}{Column name whose distinct values define the groups held out
together in \code{group_cv()}.}

\item{times}{Integer number of bootstrap resamples (default \code{25}).}
}
\value{
An object of class \code{survalis_resampling}.
}
\description{
These constructors return a lightweight \code{survalis_resampling} description that
the verb-level functions turn into concrete train/test splits. They do not
touch the data themselves, so the same specification can be reused across
models to obtain paired comparisons (identical folds via a shared \code{seed}).
}
\details{
\itemize{
\item \code{cv()} -- k-fold cross-validation, optionally repeated, stratified on the
event indicator by default.
\item \code{holdout()} -- a single stratified train/test split.
\item \code{group_cv()} -- k-fold cross-validation where whole groups (e.g. centres,
subjects with repeated rows) are held out together.
\item \code{bootstrap()} -- resampling with replacement; the out-of-bag rows form the
assessment set (Efron-Tibshirani style, suitable for \code{.632} reporting).
}
}
\examples{
cv(5)
cv(v = 10, repeats = 3, seed = 1)
holdout(prop = 0.8)
bootstrap(times = 50)
}
