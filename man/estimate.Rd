% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-estimate.R
\name{estimate}
\alias{estimate}
\title{Standardised causal-style contrast for a binary treatment}
\usage{
estimate(
  formula,
  data,
  model,
  estimand = c("RMST_diff", "RMST_ratio", "survival_diff", "survival_ratio"),
  treatment,
  tau,
  times = NULL,
  spec = list(),
  R = 200,
  conf = 0.95,
  refit = TRUE,
  seed = NULL,
  ...
)
}
\arguments{
\item{formula}{A survival formula whose right-hand side lists the
\strong{confounders} (the treatment is added by the function, with
treatment-by-confounder interactions).}

\item{data}{A data frame.}

\item{model}{A learner id (\code{list_survlearners()$learner}).}

\item{estimand}{One of \code{"RMST_diff"} (default), \code{"RMST_ratio"},
\code{"survival_diff"}, \code{"survival_ratio"}.}

\item{treatment}{Name of a binary column in \code{data}. The larger value is the
"treated" (A = 1) level.}

\item{tau}{Horizon: the RMST restriction time for the RMST estimands, or the
evaluation time for the survival estimands.}

\item{times}{Prediction grid; \code{NULL} uses \code{\link[=default_times]{default_times()}}. Must reach \code{tau}.}

\item{spec}{Engine arguments for \verb{fit_<model>()} (named list).}

\item{R}{Bootstrap replicates (default \code{200}).}

\item{conf}{Confidence level (default \code{0.95}).}

\item{refit}{Logical; refit the model inside each bootstrap replicate
(default \code{TRUE}).}

\item{seed}{Optional RNG seed.}

\item{...}{Unused.}
}
\value{
A \code{survalis_estimate} object with \code{estimand}, \code{estimate},
\code{conf.low}, \code{conf.high}, \code{conf}, \code{treatment}, \code{levels}, \code{tau}, \code{R},
\code{refit}, \code{boot} (replicate estimates) and \code{curves} (counterfactual mean
survival for each arm on \code{times}).
}
\description{
Estimates a covariate-adjusted marginal contrast between the two levels of a
binary \code{treatment} by G-computation (outcome standardisation): a survival
model is fitted for \code{Surv(time, status) ~ treatment * (confounders)}, every
subject's outcome is predicted under both treatment levels, and the
counterfactual mean survival curves are contrasted.
}
\details{
Uncertainty is a nonparametric bootstrap over subjects; by default the model
is refitted in each replicate (\code{refit = TRUE}), which propagates model
estimation error. Set \code{refit = FALSE} for a faster interval that is
conditional on the fitted model.
}
\section{Interpretation}{

The estimate has a causal reading only under standard identification
assumptions: consistency, no unmeasured confounding given the \code{formula}
covariates, positivity, no interference, and a correctly specified outcome
model. For a non-manipulable attribute it is a covariate-adjusted marginal
contrast, not a causal effect. This is a plug-in G-formula estimator; it is
not doubly robust.
}

\examples{
\dontshow{if (requireNamespace("survival", quietly = TRUE)) withAutoprint(\{ # examplesIf}
est <- estimate(Surv(time, status) ~ age + karno + celltype, veteran,
                model = "coxph", estimand = "RMST_diff",
                treatment = "trt", tau = 300, R = 50, seed = 1)
est
\dontshow{\}) # examplesIf}
}
