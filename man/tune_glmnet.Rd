% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_glmnet.R
\name{tune_glmnet}
\alias{tune_glmnet}
\title{Tune Penalized Cox Proportional Hazards Model via Cross-Validation}
\usage{
tune_glmnet(
  formula,
  data,
  times,
  param_grid = c(alpha = seq(0, 1, by = 0.25)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{times}{Numeric vector of time points at which to evaluate performance.}

\item{param_grid}{A \code{data.frame} or list specifying candidate \code{alpha} values.
Typically created with \code{\link[base]{expand.grid}}.}

\item{metrics}{Character vector of performance metrics to compute. The first
entry is used as the primary selection metric.}

\item{folds}{Integer; number of cross-validation folds. Default is \code{5}.}

\item{seed}{Integer random seed for reproducibility. Default is \code{123}.}

\item{ncores}{Integer number of CPU cores passed to \code{\link{cv_survlearner}}
for fold evaluation (default \code{1}).}

\item{refit_best}{Logical; if \code{TRUE}, fits the best configuration on the
full dataset and returns the fitted model. If \code{FALSE} (default), returns
a results table.}

\item{...}{Additional arguments passed to \code{\link{fit_glmnet}}.}
}
\value{
If \code{refit_best = FALSE}, returns a \code{data.frame} (class \code{"tuned_surv"})
with hyperparameters and metric values for each grid combination, sorted by
the primary metric.
If \code{refit_best = TRUE}, returns a fitted \code{"mlsurv_model"} from
\code{\link{fit_glmnet}}.
}
\description{
Performs hyperparameter tuning for penalized Cox models (\pkg{glmnet}) over
a grid of \code{alpha} values using cross-validation on one or more metrics.
Optionally refits the best model on the full dataset.
}
\examples{

param_grid <- expand.grid(alpha = seq(0, 1, by = 0.25))

# Evaluate grid without refitting
res_glmnet <- tune_glmnet(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = param_grid,
  metrics = c("cindex", "ibs"),
  folds = 3
)
print(res_glmnet)

# Refit best model directly
mod_glmnet_best <- tune_glmnet(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = param_grid,
  refit_best = TRUE
)

summary(mod_glmnet_best)

}
\seealso{
\code{\link{fit_glmnet}}, \code{\link{predict_glmnet}}
}
