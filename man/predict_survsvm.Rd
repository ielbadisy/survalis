% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_survsvm.R
\name{predict_survsvm}
\alias{predict_survsvm}
\title{Predict Survival Probabilities with Survival SVM}
\usage{
predict_survsvm(object, newdata, times, dist = "exp", shape = 1)
}
\arguments{
\item{object}{A fitted \code{mlsurv_model} returned by \code{\link[=fit_survsvm]{fit_survsvm()}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction. Must include
the same predictor variables used at training time.}

\item{times}{Numeric vector of evaluation time points (same scale as the
survival time used at training). Must be non-negative and finite.}

\item{dist}{Parametric family used to map predicted times to survival curves:
\code{"exp"} (exponential) or \code{"weibull"}.}

\item{shape}{Weibull shape parameter (used only if \code{dist = "weibull"}).}
}
\value{
A base \code{data.frame} with one row per observation in \code{newdata} and
columns named \code{t={time}} (character), containing numeric values in [0, 1].
}
\description{
Generates survival probabilities at specified times from a fitted survival
SVM (\code{mlsurv_model}). Since many SVM variants output a single predicted time
(or rank), this function maps predicted times to survival curves using either
an Exponential or Weibull parametric assumption.
}
\details{
\strong{Parametric mapping.} Let \eqn{\hat{T}} be the predicted time from
the SVM model (per row). For a requested evaluation time \eqn{t}:
\itemize{
\item Exponential: \eqn{S(t) = \exp(-t / \hat{T})}
\item Weibull:     \eqn{S(t) = \exp\{-(t / \hat{T})^{\kappa}\}}, where \eqn{\kappa} is \code{shape}
}
This mapping provides calibrated-looking survival probabilities but is a
modeling assumption external to the SVM fit; verify adequacy in practice.
}
\examples{
mod_svm <- fit_survsvm(Surv(time, status) ~ age + celltype + karno,
                           data = veteran,
                           type = "regression",
                           gamma.mu = 0.1,
                           kernel = "lin_kernel")

times <- c(100, 300, 500)
predict_survsvm(mod_svm, newdata = veteran[1:5, ], times = times, dist = "exp")
predict_survsvm(mod_svm, newdata = veteran[1:5, ], times = times, dist = "weibull", shape = 1.5)

}
\seealso{
\code{\link[=fit_survsvm]{fit_survsvm()}}, \code{\link[=tune_survsvm]{tune_survsvm()}}
}
