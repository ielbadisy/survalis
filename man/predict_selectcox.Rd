% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_selectcox.R
\name{predict_selectcox}
\alias{predict_selectcox}
\title{Predict Survival Probabilities with a Selected Cox Model}
\usage{
predict_selectcox(object, newdata, times)
}
\arguments{
\item{object}{A fitted \code{mlsurv_model} returned by \code{\link[=fit_selectcox]{fit_selectcox()}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction. Must include
the same predictor variables used at training time.}

\item{times}{Numeric vector of evaluation time points (same scale as the
training survival time). Must be non‑negative and finite.}
}
\value{
A base \code{data.frame} with one row per observation in \code{newdata} and
columns named \code{t={time}} (character), containing numeric survival
probabilities in [0, 1].
}
\description{
Generates survival probabilities at specified time points using a fitted
selected‑predictor Cox model (\code{mlsurv_model}) via \code{pec::predictSurvProb()}.
}
\details{
Internally calls \code{pec::predictSurvProb(object$model, newdata, times)}
and renames columns to the standard \code{t=...} convention used by \code{survalis}.
}
\examples{
mod <- fit_selectcox(Surv(time, status) ~ age + celltype + karno, data = veteran)
predict_selectcox(mod, newdata = veteran[1:5, ], times = c(100, 200, 300))

}
\seealso{
\code{\link[=fit_selectcox]{fit_selectcox()}}, \code{\link[=tune_selectcox]{tune_selectcox()}}
}
