% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_coxph.R
\name{predict_coxph}
\alias{predict_coxph}
\title{Predict Survival Probabilities from a Cox PH Model}
\usage{
predict_coxph(object, newdata, times)
}
\arguments{
\item{object}{An \code{"mlsurv_model"} object returned by
\code{fit_coxph()}.}

\item{newdata}{A data frame containing the predictor variables for
which to compute predictions.}

\item{times}{A numeric vector of time points at which to evaluate
survival probabilities.}
}
\value{
A data frame of survival probabilities with columns named
\code{"t=<time>"} for each requested time.
}
\description{
Generates predicted survival probabilities at specified time points
for new data, using a fitted Cox proportional hazards model.
}
\details{
Predictions are computed using \code{pec::predictSurvProb()}.
The output is formatted as a data frame with one row per observation
in \code{newdata} and one column per time point.
}
\examples{
\dontshow{if (requireNamespace("pec", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod_cox <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
predict_coxph(mod_cox, newdata = veteran[1:5, ], times = c(100, 200, 300))
\dontshow{\}) # examplesIf}
}
