% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-predict.R
\name{predict.survalis_fit}
\alias{predict.survalis_fit}
\title{Predict from a fitted survival learner}
\usage{
\method{predict}{survalis_fit}(
  object,
  newdata = NULL,
  times = NULL,
  type = c("survival", "risk", "chf", "hazard", "rmst", "quantile", "median"),
  ...
)
}
\arguments{
\item{object}{A \code{survalis_fit} from \code{\link[=fit]{fit()}}.}

\item{newdata}{A data frame of predictors. Defaults to the training data.
Rows with missing values in model variables are rejected.}

\item{times}{Numeric evaluation grid. When \code{NULL}, \code{\link[=default_times]{default_times()}} is used
and recorded on the result.}

\item{type}{One of:
\describe{
\item{\code{"survival"}}{predicted survival probability \eqn{S(t)} (default).}
\item{\code{"risk"}}{cumulative risk \eqn{1 - S(t)}.}
\item{\code{"chf"}}{cumulative hazard \eqn{-\log S(t)}.}
\item{\code{"hazard"}}{piecewise-constant hazard on the grid.}
\item{\code{"rmst"}}{restricted mean survival time; upper limit \code{tau} (\code{...}),
default \code{max(times)}.}
\item{\code{"quantile"}}{survival-time quantile(s); requires \code{probs} (\code{...}).}
\item{\code{"median"}}{median survival time (\code{probs = 0.5}).}
}}

\item{...}{Type-specific arguments: \code{tau} for \code{"rmst"}, \code{probs} for
\code{"quantile"}.}
}
\value{
A \link{survmat} for \code{"survival"}, \code{"risk"}, \code{"chf"}, \code{"hazard"}; a numeric
vector for \code{"rmst"}, \code{"median"} and scalar-\code{probs} \code{"quantile"}; an
\verb{n x length(probs)} matrix for vector \code{probs}.
}
\description{
Produces predictions from a \code{\link[=fit]{fit()}} object. Every matrix-valued \code{type}
returns a \link{survmat} (an \verb{n x length(times)} numeric matrix carrying its grid
in the \code{"times"} attribute); the scalar-valued types (\code{"rmst"}, \code{"quantile"},
\code{"median"}) return a numeric vector (or an \verb{n x length(probs)} matrix for a
vector \code{probs}).
}
\examples{
\dontshow{if (requireNamespace("ranger", quietly = TRUE)) withAutoprint(\{ # examplesIf}
m <- fit(Surv(time, status) ~ age + karno, veteran, "ranger",
         spec = list(num.trees = 100))
predict(m, veteran[1:5, ], times = c(90, 180, 365))
predict(m, veteran[1:5, ], times = c(90, 180, 365), type = "risk")
predict(m, veteran[1:5, ], type = "rmst", tau = 365)
\dontshow{\}) # examplesIf}
}
