% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_cforest.R
\name{tune_cforest}
\alias{tune_cforest}
\title{Tune a Conditional Inference Survival Forest}
\usage{
tune_cforest(
  formula,
  data,
  times,
  param_grid = expand.grid(ntree = c(100, 300), mtry = c(2, 3), mincriterion = c(0,
    0.95), fraction = c(0.5, 0.632)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A \code{Surv()} survival formula specifying the outcome and predictors.}

\item{data}{A data frame containing the variables in the model.}

\item{times}{A numeric vector of time points at which to evaluate performance.}

\item{param_grid}{A data frame or list specifying the grid of hyperparameter
values to evaluate. Columns should include \code{ntree}, \code{mtry}, \code{mincriterion},
and \code{fraction}.}

\item{metrics}{A character vector of performance metrics to compute
(default = \code{c("cindex", "ibs")}).}

\item{folds}{Integer, number of cross-validation folds (default = \code{5}).}

\item{seed}{Integer, random seed for reproducibility.}

\item{ncores}{Integer number of CPU cores passed to \code{\link{cv_survlearner}}
for fold evaluation (default \code{1}).}

\item{refit_best}{Logical, if \code{TRUE} refits a model with the best-performing
hyperparameters and returns it; otherwise returns a summary table of results.}

\item{...}{Additional arguments passed to \code{\link[=fit_cforest]{fit_cforest()}}.}
}
\value{
If \code{refit_best = FALSE}, a data frame of mean cross-validation scores
for each hyperparameter combination (class \code{"tuned_surv"}).
If \code{refit_best = TRUE}, an \code{mlsurv_model} object fitted with the optimal
hyperparameters.
}
\description{
Performs cross-validated hyperparameter tuning for a conditional inference
survival forest using the \code{fit_cforest()} and \code{predict_cforest()} functions.
}
\details{
Cross-validation is performed using \code{cv_survlearner()} and results
are sorted in descending order of the first metric specified in \code{metrics}.
}
\examples{
\dontshow{if (requireNamespace("party", quietly = TRUE)) withAutoprint(\{ # examplesIf}
grid <- expand.grid(
  ntree = c(50, 100),
  mtry = c(2, 4),
  mincriterion = c(0, 0.95),
  fraction = c(0.632)
)
res <- tune_cforest(Surv(time, status) ~ age + celltype + karno,
  data = veteran,
  times = c(100, 200),
  param_grid = grid,
  folds = 3
)
print(res)
\dontshow{\}) # examplesIf}
}
