% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pdp.R
\name{compute_pdp}
\alias{compute_pdp}
\title{Partial Dependence and ICE for Survival Predictions}
\usage{
compute_pdp(model, data, feature, times, method = "pdp+ice", grid.size = 20)
}
\arguments{
\item{model}{An \code{mlsurv_model} created by a \verb{fit_*()} function; must contain a
valid \code{learner} so the appropriate \verb{predict_<learner>()} can be dispatched.}

\item{data}{A data frame used to construct PDP/ICE profiles (typically the
training data).}

\item{feature}{Character scalar; the feature name to analyze (numeric or categorical).}

\item{times}{Numeric vector of evaluation times at which survival probabilities
are computed.}

\item{method}{One of \code{"pdp"}, \code{"ice"}, or \code{"pdp+ice"} (default). Controls which
profiles are produced.}

\item{grid.size}{Integer number of grid points for numeric features (default 20).
Ignored for categorical features (levels are used).}
}
\value{
A list with elements:
\describe{
\item{results}{Long data frame with columns:
\code{surv_prob}, \code{time}, \code{type} (pdp/ice),
\code{.id} (row id for ICE), and the analyzed \code{feature}.}
\item{pdp_integrated}{(Optional) Data frame with \code{feature} and
\code{integrated_surv} (time-integrated PDP), present only if
\code{length(times) > 1} and PDP was requested.}
}
}
\description{
Computes partial dependence (PDP) and/or individual conditional expectation (ICE)
curves of predicted survival probabilities for a single feature at one or more
evaluation times. Works with any learner fitted via \verb{fit_*()} that exposes a
matching \verb{predict_*()} method returning survival probabilities.
}
\details{
For numeric features, a regular grid over the observed range is used; for
categorical features, all observed levels are used. For each grid value,
predictions are made for every row of \code{data} with the feature forced to the
grid value, yielding ICE curves per row and PDP as the average across rows.

If multiple \code{times} are supplied, outputs are stacked in long format with a
\code{time} column; an additional integrated PDP is computed via trapezoidal rule
when more than one time is provided.
}
\examples{
mod <- fit_coxph(Surv(time, status) ~ age + karno + trt, data = veteran)
pdp_age <- compute_pdp(
  model = mod,
  data = veteran,
  feature = "age",
  times = c(80, 160),
  method = "pdp+ice",
  grid.size = 8
)
head(pdp_age$results)

}
