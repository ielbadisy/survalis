% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_calibration.R
\name{plot_calibration}
\alias{plot_calibration}
\title{Plot Calibration Curve for Survival Predictions}
\usage{
plot_calibration(calib_output, smooth = TRUE)
}
\arguments{
\item{calib_output}{Output list returned by \code{\link[=compute_calibration]{compute_calibration()}}.}

\item{smooth}{Logical; if \code{TRUE}, overlays a LOESS smooth of
\code{observed_surv ~ mean_pred_surv}.}
}
\value{
A \code{ggplot2} object showing bin-wise calibration points, bootstrap
error bars, the 45° reference line, and (optionally) a smooth curve.
}
\description{
Produces a calibration plot comparing mean predicted survival (x-axis) to
observed survival with bootstrap CIs (y-axis) at a single evaluation time.
}
\details{
Points above the diagonal indicate underprediction (observed survival higher
than predicted), while points below indicate overprediction.
}
\examples{
mod <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
calib <- compute_calibration(
  model = mod,
  data = veteran,
  time = "time",
  status = "status",
  eval_time = 80,
  n_bins = 4,
  n_boot = 5,
  seed = 1
)
plot_calibration(calib)

}
\seealso{
\code{\link[=compute_calibration]{compute_calibration()}}
}
