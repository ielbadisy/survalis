% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_tree_surrogate.R
\name{plot_tree_surrogate}
\alias{plot_tree_surrogate}
\title{Plot Tree-Based Surrogate Models or Feature Importances}
\usage{
plot_tree_surrogate(tree_surrogate, type = c("tree", "importance"), top_n = 10)
}
\arguments{
\item{tree_surrogate}{An object of class \code{"tree_surrogate"}.}

\item{type}{Character string indicating the type of plot:
\itemize{
\item \code{"tree"}: displays the surrogate decision tree(s) for each time.
\item \code{"importance"}: displays a bar plot of top features ranked by split count.
}}

\item{top_n}{Integer, the number of top features to display in the importance plot.
Ignored if \code{type = "tree"}.}
}
\value{
A plot object (for \code{"importance"}) or printed tree diagrams (for \code{"tree"}).
}
\description{
Visualizes the results of a \code{tree_surrogate} object returned by
\code{\link[=compute_tree_surrogate]{compute_tree_surrogate()}}. Can display either the fitted surrogate trees
or aggregated feature importance based on split counts.
}
\details{
\itemize{
\item If \code{type = "tree"}, a separate tree diagram is produced for each evaluation time.
\item If \code{type = "importance"}, feature split counts are summed across all times
and plotted as a bar chart.
}

Requires the \code{partykit} package for tree plotting and \code{ggplot2} for importance plotting.
}
\examples{
\donttest{
plot_tree_surrogate(tree_ranger, type = "tree")
plot_tree_surrogate(tree_ranger, type = "importance", top_n = 5)
}

}
