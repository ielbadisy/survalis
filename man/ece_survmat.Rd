% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{ece_survmat}
\alias{ece_survmat}
\title{Expected Calibration Error (ECE) at a Single Time Point}
\usage{
ece_survmat(object, sp_matrix, t_star, n_bins = 10L, p = 1, weighted = TRUE)
}
\arguments{
\item{object}{A \code{\link[survival]{Surv}} object.}

\item{sp_matrix}{Matrix or data frame of survival probabilities with rows =
subjects and (optionally) columns named \code{"t=<time>"}.}

\item{t_star}{Numeric evaluation time (must be a single value).}

\item{n_bins}{Integer number of quantile bins (default \code{10}).}

\item{p}{Power for the L^p error (default \code{1} gives absolute error).}

\item{weighted}{Logical; if \code{TRUE} (default) weights bin errors by
\code{n_k / N} where \code{n_k} is the bin size.}
}
\value{
A named numeric scalar: \code{"ece"}.
}
\description{
Computes a binned Expected Calibration Error at time \code{t_star}, using
quantile-based bins of predicted survival probabilities.
}
\details{
Let \eqn{\hat{S}_k(t^*)} be the mean predicted survival in bin \eqn{k} and
\eqn{\tilde{S}_k(t^*)} the Kaplan-Meier survival at \eqn{t^*} in that bin.
The (weighted) ECE is
\deqn{
  \mathrm{ECE}(t^*) = \left(
    \sum_k w_k \, |\hat{S}_k(t^*) - \tilde{S}_k(t^*)|^p
  \right)^{1/p}
}
with \eqn{w_k = n_k / N}.
}
\examples{
y <- survival::Surv(
  time = c(1, 2, 3, 4, 6, 7, 8, 9),
  event = c(1, 1, 0, 1, 0, 1, 1, 0)
)
sp <- cbind("t=5" = c(0.15, 0.20, 0.35, 0.40, 0.55, 0.65, 0.75, 0.80))
ece_survmat(y, sp_matrix = sp, t_star = 5, n_bins = 4)
}
