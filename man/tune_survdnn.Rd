% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_survdnn.R
\name{tune_survdnn}
\alias{tune_survdnn}
\title{Tune Deep Neural Network Survival Models (Cross-Validation)}
\usage{
tune_survdnn(
  formula,
  data,
  times,
  param_grid = list(hidden = list(c(32, 16), c(64, 32, 16)), lr = c(0.001, 5e-04),
    activation = c("relu", "gelu"), epochs = c(100, 200), loss = c("cox", "aft")),
  metrics = c("cindex", "ibs"),
  folds = 3,
  seed = 42,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.
The left-hand side must be a \code{Surv()} object from the \code{survival} package.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{times}{Numeric vector of evaluation time points (same scale as the
survival time used at training). Must be non-negative and finite.}

\item{param_grid}{A named list of candidate hyperparameters. Typical entries:
\code{hidden} (list of integer vectors), \code{lr} (numeric), \code{activation} (character),
\code{epochs} (integer), and \code{loss} (character).}

\item{metrics}{Character vector of metrics to evaluate/optimize
(e.g., \code{"cindex"}, \code{"ibs"}). The first entry is used as the primary
selection metric.}

\item{folds}{Number of cross-validation folds.}

\item{seed}{Integer random seed for reproducibility.}

\item{refit_best}{Logical; if \code{TRUE}, refits the best configuration on the
full data and returns it as the result (with tuning attributes).}

\item{...}{Additional arguments forwarded to the underlying engine where applicable.}
}
\value{
If \code{refit_best = FALSE}, a \code{data.frame} (class \code{"tuned_surv"}) with
hyperparameter settings and metric columns, ordered by the first metric.
If \code{refit_best = TRUE}, a fitted \code{mlsurv_model} from \code{\link[=fit_survdnn]{fit_survdnn()}} with
attribute \code{"tuning_results"} containing the full grid results.
}
\description{
Cross-validates \pkg{survdnn}-based models over a user-specified grid and
selects the best configuration by the primary metric. Optionally refits the
best model on the full dataset.
}
\details{
\strong{Evaluation.} Internally calls \code{cv_survlearner()} with
\code{fit_survdnn()}/\code{predict_survdnn()} so tuning uses the same code paths as
production. Hyperparameters are combined via \code{tidyr::crossing()} and
iterated with \code{purrr::pmap_dfr()}.
}
\examples{
\donttest{

grid <- list(
  hidden = list(c(16), c(32, 16)),
  lr = c(1e-4, 5e-4),
  activation = c("relu", "tanh"),
  epochs = c(300),
  loss = c("cox", "coxtime")
)

mod <- tune_survdnn(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(90),
  metrics = c("cindex", "ibs"),
  param_grid = grid,
  refit_best = TRUE
)

summary(mod)
}

}
\seealso{
\code{\link[=fit_survdnn]{fit_survdnn()}}, \code{\link[=predict_survdnn]{predict_survdnn()}}
}
