% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-helpers.R
\name{survmat_to_chf}
\alias{survmat_to_chf}
\title{Convert a survival-probability matrix (survmat) to cumulative hazard}
\usage{
survmat_to_chf(S, eps = 1e-12)
}
\arguments{
\item{S}{A \code{surv_mat}: numeric matrix/data.frame of survival probabilities.}

\item{eps}{Numeric in (0,1). Stabilizer to avoid \code{log(0)}. Values of
\code{S} are clamped to \eqn{[\varepsilon, 1-\varepsilon]} before logging.
Default is \code{1e-12}.}
}
\value{
A numeric matrix with the same dimensions as \code{S}, containing
cumulative hazards \eqn{\Lambda(t \mid x)}.
}
\description{
Uses the identity \eqn{\Lambda(t \mid x) = -\log S(t \mid x)} to compute
cumulative hazards for each observation and time point.
}
\examples{
S <- data.frame(`t=1` = c(0.9, 0.8), `t=2` = c(0.7, 0.6))
survmat_to_chf(S)

}
