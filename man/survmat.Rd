% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-class.R
\name{survmat}
\alias{survmat}
\alias{new_survmat}
\alias{validate_survmat}
\alias{as_survmat}
\title{The survmat prediction contract}
\usage{
new_survmat(x, times, quantity = "survival")

validate_survmat(x)

as_survmat(x, times, quantity = "survival")
}
\arguments{
\item{x}{A numeric matrix (or data frame / vector coercible to one) of
predictions, one row per observation and one column per entry of \code{times}.}

\item{times}{Numeric vector, strictly increasing and non-negative, of length
\code{ncol(x)}.}

\item{quantity}{One of \code{"survival"}, \code{"risk"}, \code{"chf"}, \code{"hazard"}. Controls
printing and the \verb{[0, 1]} range check (applied to \code{"survival"} and
\code{"risk"} only).}
}
\value{
A \code{survmat}: a numeric matrix with class \code{c("survmat", "matrix", "array")}, attribute \code{"times"}, and attribute \code{"quantity"}.
}
\description{
Internal constructor and validator for \code{survmat}, the single prediction
container used by the verb-level \code{\link[=predict]{predict()}} method. A \code{survmat} is an
\verb{n x length(times)} numeric matrix carrying its evaluation grid in the
\code{"times"} attribute (column names \code{"t=<time>"} are kept for display and for
backward compatibility with the \verb{*_survmat} metric helpers) and the predicted
quantity in the \code{"quantity"} attribute.
}
\details{
\code{new_survmat()} builds the object without checks; \code{validate_survmat()} enforces
the contract; \code{as_survmat()} is the checked constructor used internally after a
learner's raw \verb{predict_*()} output has been finalised.
}
\keyword{internal}
