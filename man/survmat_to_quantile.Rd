% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-helpers.R
\name{survmat_to_quantile}
\alias{survmat_to_quantile}
\title{Compute a survival-time quantile from a survival-probability matrix (survmat) with grid-based approach}
\usage{
survmat_to_quantile(S, times, p = 0.5)
}
\arguments{
\item{S}{A \code{surv_mat}: numeric matrix/data.frame of survival probabilities.}

\item{times}{Numeric vector of time points corresponding to columns of \code{S}.
Must be strictly increasing and non-negative.}

\item{p}{Probability in (0,1). Default is \code{0.5}.}
}
\value{
Numeric vector of quantile times (length = \code{nrow(S)}). Returns
\code{NA} if the threshold is not crossed on the grid (e.g., survival stays
above \eqn{1-p} over all \code{times}).
}
\description{
Returns the smallest grid time \eqn{t} such that \eqn{S(t) \le 1-p}.
This is a right-continuous approximation on the provided time grid.
}
\details{
For \code{p = 0.5}, this yields a grid-based \strong{median survival time}.
}
\examples{
times <- c(1, 2, 3)
S <- matrix(c(0.9, 0.6, 0.4,
              0.95,0.9,0.85), nrow = 2, byrow = TRUE)
colnames(S) <- paste0("t=", times)
survmat_to_quantile(S, times, p = 0.5)  # median on the grid

}
