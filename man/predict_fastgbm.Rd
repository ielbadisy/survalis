% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_fastgbm.R
\name{predict_fastgbm}
\alias{predict_fastgbm}
\title{Predict Survival Probabilities from a fastgbm Model}
\usage{
predict_fastgbm(object, newdata, times)
}
\arguments{
\item{object}{An \code{"mlsurv_model"} from \code{\link{fit_fastgbm}}.}

\item{newdata}{A \code{data.frame} of predictors.}

\item{times}{Numeric vector of evaluation time points.}
}
\value{
A \code{data.frame} of survival probabilities with one row per
observation in \code{newdata} and one column per time point (columns named
\code{"t=<time>"}).
}
\description{
Generates predicted survival probabilities at the requested \code{times} for a
model fitted with \code{\link{fit_fastgbm}} (piecewise-exponential objective).
}
\examples{
\dontshow{if (requireNamespace("fastgbm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_fastgbm(Surv(time, status) ~ age + karno, veteran, ntrees = 50)
predict_fastgbm(mod, veteran[1:5, ], times = c(100, 200, 300))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{fit_fastgbm}}
}
