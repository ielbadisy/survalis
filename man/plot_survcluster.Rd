% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_survcluster.R
\name{plot_survcluster}
\alias{plot_survcluster}
\title{Plot Clustered Survival Curves}
\usage{
plot_survcluster(survcluster_output, title)
}
\arguments{
\item{survcluster_output}{Output list returned by \code{\link[=compute_survcluster]{compute_survcluster()}}.}

\item{title}{Plot title. If missing, an automatically generated title is
used. Pass \code{NULL} to omit the title entirely (e.g., for journals
requiring caption-only figures).}
}
\value{
A \code{ggplot2} object showing per-subject survival curves colored by
cluster, with cluster medoid curves overlaid.
}
\description{
Plots the individualized survival curves from \code{\link[=compute_survcluster]{compute_survcluster()}},
colored by cluster membership, with medoid curves overlaid.
}
\examples{
if (requireNamespace("unsurv", quietly = TRUE) &&
    requireNamespace("cluster", quietly = TRUE)) {
  mod <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
  time_grid <- seq(10, 300, length.out = 20)
  cl <- compute_survcluster(mod, newdata = veteran, times = time_grid, K = 2, seed = 1)
  plot_survcluster(cl)
}

}
\seealso{
\code{\link[=compute_survcluster]{compute_survcluster()}}
}
