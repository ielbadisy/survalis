% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survmat-helpers.R
\name{survmat_to_rmst}
\alias{survmat_to_rmst}
\title{Compute restricted mean survival time (RMST) from a survival-probability matrix (survmat)}
\usage{
survmat_to_rmst(S, times, tau = max(times))
}
\arguments{
\item{S}{A \code{surv_mat}: numeric matrix/data.frame of survival probabilities.}

\item{times}{Numeric vector of time points corresponding to columns of \code{S}.
Must be strictly increasing and non-negative.}

\item{tau}{Positive numeric scalar; upper integration limit. Default is \code{max(times)}.}
}
\value{
Numeric vector of RMST values (length = \code{nrow(S)}).
}
\description{
Computes \eqn{\mathrm{RMST}(\tau) = \int_0^\tau S(t)\, dt} for each observation,
approximated via the trapezoidal rule on the provided time grid.
}
\details{
If the grid does not include \code{0}, the function prepends \eqn{(0, S(0)=1)}
to correctly integrate from time 0.
}
\examples{
times <- c(1, 2, 3)
S <- matrix(c(0.9, 0.8, 0.7,
              0.95,0.9,0.85), nrow = 2, byrow = TRUE)
colnames(S) <- paste0("t=", times)
survmat_to_rmst(S, times, tau = 3)

}
