% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_default_survlearners.R
\name{best_survlearner}
\alias{best_survlearner}
\title{Select the Best Survival Learner by a Given Metric}
\usage{
best_survlearner(benchmark_results, metric, maximize = NULL)
}
\arguments{
\item{benchmark_results}{A data frame with columns \code{learner},
\code{metric}, and \code{value}.}

\item{metric}{Character name of the metric to optimize (e.g., \code{"cindex"},
\code{"ibs"}). Must exist in \code{benchmark_results$metric}.}

\item{maximize}{Logical; whether to maximize the metric. If \code{NULL}
(default), the function maximizes for concordance‑like metrics
(\code{"cindex"}) and minimizes for error‑like metrics
(\code{"ibs"}, \code{"brier"}, \code{"iae"}, \code{"ise"}, \code{"ece"}).}
}
\value{
A tibble with columns \code{learner}, \code{metric}, and the selected
average \code{value} for the best learner(s). Ties are returned as multiple rows.
}
\description{
Extracts the top‑performing learner(s) under a chosen metric from benchmark
results, using the average value across folds.
}
\examples{
res <- tibble::tibble(
  learner = c("coxph", "coxph", "rpart", "rpart"),
  metric = c("cindex", "ibs", "cindex", "ibs"),
  value = c(0.64, 0.19, 0.60, 0.23)
)
best_survlearner(res, metric = "cindex")
best_survlearner(res, metric = "ibs")

}
\seealso{
\code{\link[=benchmark_default_survlearners]{benchmark_default_survlearners()}}, \code{\link[=summarise_benchmark]{summarise_benchmark()}}
}
