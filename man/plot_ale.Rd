% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ale.R
\name{plot_ale}
\alias{plot_ale}
\title{Plot ALE Curves for Survival Models}
\usage{
plot_ale(
  ale_result,
  feature,
  which = c("per_time", "integrated"),
  smooth = FALSE
)
}
\arguments{
\item{ale_result}{A list returned by \code{\link[=compute_ale]{compute_ale()}}.}

\item{feature}{Character name of the feature (for axis labeling only).}

\item{which}{Either \code{"per_time"} to plot one ALE curve per time, or
\code{"integrated"} to plot the time-averaged ALE (requires multiple \code{times}).}

\item{smooth}{Logical; if \code{TRUE}, overlays a LOESS smooth on the plotted
curve(s).}
}
\value{
A \code{ggplot2} object.
}
\description{
Visualizes ALE results produced by \code{\link[=compute_ale]{compute_ale()}} either as per-time curves
(one curve per evaluation time) or as an integrated curve averaged across
times.
}
\details{
Per-time plots show how the feature's local effect varies across different
evaluation times. The integrated plot summarizes the average effect over the
supplied time grid (simple mean across times of the centered ALE values).
}
\examples{
mod <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
ale_res <- compute_ale(
  model = mod,
  newdata = veteran,
  feature = "karno",
  times = c(80, 160),
  grid.size = 8
)
plot_ale(ale_res, feature = "karno", which = "per_time")
plot_ale(ale_res, feature = "karno", which = "integrated", smooth = TRUE)

}
\seealso{
\code{\link[=compute_ale]{compute_ale()}}, \code{\link[=compute_pdp]{compute_pdp()}}, \code{\link[=plot_pdp]{plot_pdp()}}
}
