% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_selectcox.R
\name{fit_selectcox}
\alias{fit_selectcox}
\title{Fit a Predictor-Selection Cox Model (pec::selectCox, mlsurv_model-compatible)}
\usage{
fit_selectcox(formula, data, rule = "aic")
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.
The left‑hand side must be a \code{Surv()} object from the \pkg{survival} package.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{rule}{Selection rule passed to \code{pec::selectCox()}, e.g., \code{"aic"}
(default) or \code{"p"}.}
}
\value{
An object of class \code{mlsurv_model}, a named list with elements:
\describe{
\item{model}{The fitted \code{pec::selectCox} model.}
\item{learner}{Character scalar identifying the learner (\code{"selectcox"}).}
\item{engine}{Character scalar naming the engine (\code{"selectCox"}).}
\item{formula}{The original survival formula.}
\item{data}{The training dataset (or a minimal subset needed for prediction).}
\item{rule}{The selection rule used.}
}
}
\description{
Fits a Cox proportional hazards model with automated predictor selection
using \pkg{pec}'s \code{selectCox()}, returning an \code{mlsurv_model} object
compatible with the \code{survalis} evaluation and cross‑validation pipeline.
}
\details{
This wrapper standardizes the return object to the \code{mlsurv_model} contract so
downstream prediction and evaluation behave consistently across learners.
}
\section{Engine}{

Uses \strong{pec::selectCox}. Selection can be driven by Akaike's Information
Criterion (\code{rule = "aic"}) or by p‑value thresholds (\code{rule = "p"}),
as implemented by \pkg{pec}.
}

\examples{
mod <- fit_cforest(Surv(time, status) ~ age + celltype + karno, data = veteran)

}
\references{
Mogensen UB, Ishwaran H, Gerds TA (2012). Evaluating Random Forests for
Survival Analysis using \pkg{pec}.
Gerds TA, et al. \pkg{pec}: Prediction Error Curves for Survival Models.
}
\seealso{
\code{\link[=predict_selectcox]{predict_selectcox()}}, \code{\link[=tune_selectcox]{tune_selectcox()}}, \code{pec::selectCox()}
}
