% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.mlsurv_model.R
\name{summary.mlsurv_model}
\alias{summary.mlsurv_model}
\title{Summarize an \code{mlsurv_model}}
\usage{
\method{summary}{mlsurv_model}(object, ...)
}
\arguments{
\item{object}{An object of class \code{"mlsurv_model"} produced by a \verb{fit_*()} function.}

\item{...}{Ignored; included for S3 signature compatibility.}
}
\value{
Invisibly returns a list of class \code{"summary.mlsurv_model"} containing:
\describe{
\item{learner}{Character id of the learner (e.g., \code{"ranger"}, \code{"coxph"}).}
\item{engine}{Underlying package/engine used to fit the model.}
\item{formula}{The original survival formula.}
\item{data_summary}{List with \code{observations}, \code{predictors},
\code{time_range}, and \code{event_rate}.}
}
}
\description{
Produces a compact, human‑readable overview of a fitted survival learner that
follows the \code{mlsurv_model} contract (e.g., objects returned by \verb{fit_*()} in
this toolkit). The summary prints the learner id, engine, original formula,
and basic data characteristics (sample size, predictor names, time range,
event rate). A structured list is returned invisibly for programmatic use.
}
\details{
This method relies on the presence of the fields \code{learner}, \code{formula},
and \code{data} stored in the fitted object (the standard \code{mlsurv_model}
contract). Output printing uses \pkg{cli} if available.
}
\examples{
mod <- fit_coxph(Surv(time, status) ~ age + trt + celltype, data = veteran)
summary(mod)

s <- summary(mod)  # capture the structured result invisibly
str(s)

}
\seealso{
\code{\link[=fit_coxph]{fit_coxph()}}, other \verb{fit_*()} learners returning \code{mlsurv_model} objects.
}
