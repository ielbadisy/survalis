% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_survcluster.R
\name{compute_survcluster}
\alias{compute_survcluster}
\title{Cluster Predicted Survival Curves}
\usage{
compute_survcluster(
  model,
  newdata,
  times,
  K = NULL,
  K_max = 10,
  distance = c("L2", "L1"),
  seed = NULL,
  ...
)
}
\arguments{
\item{model}{An \code{mlsurv_model} fitted via a \verb{fit_*()} function; must include a
valid \code{learner} so the corresponding \verb{predict_<learner>()} can be dispatched.}

\item{newdata}{A data frame with predictors for the subjects to cluster.}

\item{times}{Numeric vector of evaluation times (the common time grid on
which survival curves are predicted and clustered).}

\item{K}{Optional integer number of clusters. If \code{NULL}, selected by
silhouette width (see \code{\link[unsurv:unsurv]{unsurv::unsurv()}}).}

\item{K_max}{Maximum \code{K} considered when \code{K} is \code{NULL}.}

\item{distance}{Distance type passed to \code{\link[unsurv:unsurv]{unsurv::unsurv()}}: \code{"L2"} or \code{"L1"}.}

\item{seed}{Optional integer seed forwarded to \code{\link[unsurv:unsurv]{unsurv::unsurv()}}.}

\item{...}{Additional arguments forwarded to \code{\link[unsurv:unsurv]{unsurv::unsurv()}} (e.g.
\code{weights}, \code{enforce_monotone}, \code{smooth_median_width}, \code{standardize_cols}).}
}
\value{
A list with components:
\describe{
\item{fit}{The fitted \code{unsurv} object (clusters, medoids, silhouette, ...).}
\item{survmat}{The numeric survival-probability matrix that was clustered.}
\item{times}{The evaluation time grid used.}
\item{learner}{The learner label (from \code{model$learner}).}
}
}
\description{
Clusters the individualized survival curves predicted by a fitted
\code{mlsurv_model} using \code{\link[unsurv:unsurv]{unsurv::unsurv()}} (Partitioning Around Medoids on a
weighted feature representation of the curves).
}
\details{
Predicted survival is obtained from the model's \verb{predict_<learner>()} on
\code{times}, coerced to a numeric matrix, and passed to \code{\link[unsurv:unsurv]{unsurv::unsurv()}}.
Requires the \pkg{unsurv} package.
}
\examples{
if (requireNamespace("unsurv", quietly = TRUE) &&
    requireNamespace("cluster", quietly = TRUE)) {
  mod <- fit_coxph(Surv(time, status) ~ age + karno + celltype, data = veteran)
  time_grid <- seq(10, 300, length.out = 20)
  cl <- compute_survcluster(mod, newdata = veteran, times = time_grid, K = 2, seed = 1)
  table(cl$fit$clusters)
}

}
\references{
El Badisy, I. (2026). unsurv: Clustering Individualized Survival Curves.
Bioinformatics Advances, vbag218. \doi{10.1093/bioadv/vbag218}
}
\seealso{
\code{\link[=plot_survcluster]{plot_survcluster()}}
}
