% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rpart.R
\name{tune_rpart}
\alias{tune_rpart}
\title{Tune a Survival Tree Model (\code{rpart}) via Cross-Validation}
\usage{
tune_rpart(
  formula,
  data,
  times,
  param_grid = expand.grid(minsplit = c(10, 20), cp = c(0.001, 0.01), maxdepth = c(10,
    30)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = TRUE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the variables in the model.}

\item{times}{Numeric vector of time points for evaluation.}

\item{param_grid}{A \code{data.frame} or \code{expand.grid()} result specifying hyperparameter combinations.
Must include \code{minsplit}, \code{cp}, and \code{maxdepth}.}

\item{metrics}{Character vector of evaluation metrics to compute (e.g., \code{"cindex"}, \code{"ibs"}).}

\item{folds}{Number of cross-validation folds. Default is \code{5}.}

\item{seed}{Random seed for reproducibility.}

\item{refit_best}{Logical; if \code{TRUE}, the best hyperparameter combination is refitted on the full dataset.}

\item{...}{Additional arguments passed to \code{\link[=fit_rpart]{fit_rpart()}}.}
}
\value{
If \code{refit_best = FALSE}, returns a tibble summarizing mean CV performance for each parameter combination.
If \code{refit_best = TRUE}, returns an \code{"mlsurv_model"} object for the best parameters, with a \code{"tuning_results"} attribute.
}
\description{
Performs hyperparameter tuning for the \code{rpart} survival tree model using
cross-validation, returning either a table of results or the best fitted model.
}
\examples{

# Example tuning without refit
res_rpart <- tune_rpart(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = expand.grid(
    minsplit = c(10, 20),
    cp = c(0.001, 0.01),
    maxdepth = c(10, 30)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3,
  seed = 42,
  refit_best = FALSE
)

print(res_rpart)

# Example tuning with refit
mod_rpart_best <- tune_rpart(
  formula = Surv(time, status) ~ age + karno + celltype,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = expand.grid(
    minsplit = c(10, 20),
    cp = c(0.001, 0.01),
    maxdepth = c(10, 30)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3,
  seed = 42,
  refit_best = TRUE
)

}
