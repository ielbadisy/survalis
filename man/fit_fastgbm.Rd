% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_fastgbm.R
\name{fit_fastgbm}
\alias{fit_fastgbm}
\title{Fit a fastgbm Survival Model}
\usage{
fit_fastgbm(
  formula,
  data,
  objective = c("pexp", "cox", "aft"),
  ntrees = 200,
  learning_rate = 0.1,
  max_depth = 5,
  pexp_bins = 10,
  ...
)
}
\arguments{
\item{formula}{A survival formula \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing the formula variables.}

\item{objective}{fastgbm survival objective: \code{"pexp"} (default),
\code{"cox"}, or \code{"aft"}. Only \code{"pexp"} supports
\code{predict(type = "survival")} at arbitrary times and is required for the
survalis prediction contract.}

\item{ntrees}{Number of boosting rounds (default \code{200}).}

\item{learning_rate}{Shrinkage (default \code{0.1}).}

\item{max_depth}{Maximum tree depth (default \code{5}).}

\item{pexp_bins}{Number of piecewise-exponential time intervals (default
\code{10}).}

\item{...}{Additional arguments passed to \code{fastgbm::fastgbm()} (for
example \code{min_node_size}, \code{subsample}, \code{colsample},
\code{lambda}, \code{early_stopping} + \code{validation}, \code{seed}).}
}
\value{
An object of class \code{"mlsurv_model"} with elements \code{model},
\code{learner} (\code{"fastgbm"}), \code{formula}, \code{data}, \code{time},
\code{status}, and \code{objective}.
}
\description{
Fits a histogram-based gradient boosting survival model with the
\pkg{fastgbm} package and returns an object compatible with the
\code{mlsurv_model} interface. The piecewise-exponential objective
(\code{objective = "pexp"}) is used by default because it models the hazard
jointly over covariates and time, so predicted survival curves can be
evaluated at arbitrary time points.
}
\examples{
\dontshow{if (requireNamespace("fastgbm", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_fastgbm(Surv(time, status) ~ age + karno + celltype, veteran,
                   ntrees = 50, pexp_bins = 8)
summary(mod)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{predict_fastgbm}}, \code{\link{tune_fastgbm}},
\code{\link[fastgbm]{fastgbm}}
}
\keyword{internal}
