% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/verb-fit.R
\name{fit}
\alias{fit}
\title{Fit a survival learner}
\usage{
fit(formula, data, model, spec = list(), seed = NULL, ...)
}
\arguments{
\item{formula}{A survival formula with a \code{Surv()} outcome on the left-hand
side, e.g. \code{Surv(time, status) ~ age + karno}.}

\item{data}{A data frame containing the formula variables.}

\item{model}{A learner id from \code{list_survlearners()$learner} (for example
\code{"coxph"}, \code{"ranger"}, \code{"survdnn"}).}

\item{spec}{A named list of engine arguments forwarded to the underlying
\verb{fit_<model>()} (for example \code{list(num.trees = 500)} for \code{"ranger"}).}

\item{seed}{Optional integer seed set immediately before fitting, for learners
with a stochastic training step.}

\item{...}{Deprecated: engine arguments passed here are merged into \code{spec}
with a one-time warning. Use \code{spec}.}
}
\value{
An object of class \code{c("survalis_fit", "mlsurv_model")} with elements
\code{model}, \code{learner}, \code{engine}, \code{formula}, \code{data}, \code{time}, \code{status},
\code{model_id}, and \code{spec}.
}
\description{
The entry point of the survalis verb interface. \code{fit()} selects a learner by
id, passes engine arguments through \code{spec}, and returns a \code{survalis_fit}
object that the other verbs (\code{\link[=predict]{predict()}}, \code{\link[=evaluate]{evaluate()}}, \code{\link[=tune]{tune()}}, \code{\link[=interpret]{interpret()}},
\code{\link[=estimate]{estimate()}}) consume.
}
\details{
\code{survalis_fit} extends the internal \code{mlsurv_model} contract, so every legacy
\verb{predict_*()} / \verb{compute_*()} helper keeps working on it unchanged.
}
\examples{
\dontshow{if (requireNamespace("ranger", quietly = TRUE)) withAutoprint(\{ # examplesIf}
m <- fit(Surv(time, status) ~ age + karno + celltype, veteran,
         model = "ranger", spec = list(num.trees = 200))
m
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=predict.survalis_fit]{predict.survalis_fit()}}, \code{\link[=evaluate]{evaluate()}}, \code{\link[=tune]{tune()}}, \code{\link[=compare]{compare()}},
\code{\link[=interpret]{interpret()}}, \code{\link[=estimate]{estimate()}}, \code{\link[=list_survlearners]{list_survlearners()}}
}
