% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bart.R
\name{fit_bart}
\alias{fit_bart}
\title{Fit a Bayesian Additive Regression Trees (BART) Survival Model}
\usage{
fit_bart(formula, data, K = 3, ...)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.
Only right‐censored responses are supported.}

\item{data}{A \code{data.frame} containing variables referenced in \code{formula}.}

\item{K}{Integer; number of internal time grid intervals used by the BART
survival engine (default \code{3}). Larger values yield a finer internal
evaluation grid.}

\item{...}{Further args to BART::surv.bart}
}
\value{
An object of class \code{mlsurv_model} with elements:
\describe{
\item{model}{Fitted \code{BART::surv.bart} object.}
\item{learner}{\code{"bart"}.}
\item{formula, data}{Original inputs.}
\item{eval_times}{Engine's internal time grid used for prediction.}
}
}
\description{
Fits a right‐censored survival model using \pkg{BART}'s
\code{\link[BART]{surv.bart}} and returns an \code{mlsurv_model} compatible with
the \pkg{survalis} pipeline.
}
\details{
The response must be of class \code{Surv(..., type = "right")}. The fitted
object stores the engine's internal evaluation times in \code{$eval_times}
(from \code{bart_fit$times}) for downstream prediction alignment.
}
\examples{
\dontshow{if (requireNamespace("BART", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
mod_bart <- fit_bart(
  Surv(time, status) ~ age + karno + celltype,
  data = veteran
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{predict_bart}}, \code{\link[BART]{surv.bart}}
}
