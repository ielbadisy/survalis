% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_densemlp.R
\name{predict_densemlp}
\alias{predict_densemlp}
\title{Predict Survival Probabilities from a densemlp Model}
\usage{
predict_densemlp(object, newdata, times)
}
\arguments{
\item{object}{An \code{"mlsurv_model"} from \code{\link{fit_densemlp}}.}

\item{newdata}{A \code{data.frame} of predictors.}

\item{times}{Numeric vector of evaluation time points.}
}
\value{
A \code{data.frame} of survival probabilities with one row per
observation in \code{newdata} and one column per time point (columns named
\code{"t=<time>"}).
}
\description{
Generates predicted survival probabilities at the requested \code{times} for a
model fitted with \code{\link{fit_densemlp}} (\code{loss = "brier"}). The
network returns survival on its own discrete-time grid; values are linearly
interpolated (and constant-extrapolated) onto \code{times}.
}
\examples{
\dontshow{if (requireNamespace("densemlp", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod <- fit_densemlp(Surv(time, status) ~ age + karno, veteran,
                    n_bins = 8, epochs = 20)
predict_densemlp(mod, veteran[1:5, ], times = c(100, 200, 300))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{fit_densemlp}}
}
\keyword{internal}
