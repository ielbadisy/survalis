% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_rsf.R
\name{tune_rsf}
\alias{tune_rsf}
\title{Tune Random Survival Forest Hyperparameters (Cross-Validation)}
\usage{
tune_rsf(
  formula,
  data,
  times,
  param_grid = expand.grid(ntree = c(200, 500), mtry = c(1, 2, 3), nodesize = c(5, 15)),
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  refit_best = FALSE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ predictors}.}

\item{data}{A \code{data.frame} containing all variables referenced in \code{formula}.}

\item{times}{Numeric vector of evaluation time points (same scale as the
survival time used at training). Must be non-negative and finite.}

\item{param_grid}{A data frame (e.g., from \code{expand.grid()}) containing columns
for RSF hyperparameters such as \code{ntree}, \code{mtry}, and \code{nodesize}.}

\item{metrics}{Character vector of metrics to evaluate/optimize
(e.g., \code{"cindex"}, \code{"ibs"}). The first entry is the primary selection metric.}

\item{folds}{Integer; number of cross-validation folds.}

\item{seed}{Integer random seed for reproducibility.}

\item{ncores}{Integer number of CPU cores passed to \code{\link{cv_survlearner}}
for fold evaluation (default \code{1}).}

\item{refit_best}{Logical; if \code{TRUE}, refits the best configuration on the
full data and returns it, with tuning results attached.}

\item{...}{Additional arguments forwarded to the underlying engine where applicable.}
}
\value{
If \code{refit_best = FALSE}, a \code{data.frame} (class \code{"tuned_surv"}) of grid
results with metric columns and hyperparameters, ordered by the first metric.
If \code{refit_best = TRUE}, a fitted \code{mlsurv_model} from \code{\link[=fit_rsf]{fit_rsf()}} with
attribute \code{"tuning_results"} containing the full grid results.
}
\description{
Cross-validates RSF models over a specified parameter grid and selects the
best configuration according to the primary metric. Optionally refits the
best model on the full dataset.
}
\details{
Internally calls \code{cv_survlearner()} with \code{fit_rsf()}/\code{predict_rsf()} so tuning
uses the same code paths as production. Typical grids vary \code{ntree}, \code{mtry},
and \code{nodesize}.
}
\examples{
\dontshow{if (requireNamespace("randomForestSRC", quietly = TRUE)) withAutoprint(\{ # examplesIf}
\donttest{
mod_rsf_best <- tune_rsf(
  formula = Surv(time, status) ~ age + celltype + karno,
  data = veteran,
  times = c(100, 200, 300),
  param_grid = expand.grid(
    ntree = c(200, 500),
    mtry = c(1, 2, 3),
    nodesize = c(5, 15)
  ),
  metrics = c("cindex", "ibs"),
  folds = 3,
  refit_best = TRUE
)

summary(mod_rsf_best)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=fit_rsf]{fit_rsf()}}, \code{\link[=predict_rsf]{predict_rsf()}}
}
