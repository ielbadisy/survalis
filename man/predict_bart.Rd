% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bart.R
\name{predict_bart}
\alias{predict_bart}
\title{Predict Survival Probabilities from a BART Survival Model}
\usage{
predict_bart(object, newdata, times)
}
\arguments{
\item{object}{An \code{mlsurv_model} returned by \code{\link{fit_bart}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction.}

\item{times}{Numeric vector of time points at which to estimate survival
probabilities (same scale as the training time).}
}
\value{
A base \code{data.frame} with one row per observation in \code{newdata}
and columns named \code{"t=<time>"} (character), containing survival
probabilities in [0, 1].
}
\description{
Generates survival probability predictions at requested times for new data
using a model fitted by \code{\link{fit_bart}}.
}
\details{
The BART engine predicts survival on its internal grid \code{object$eval_times}.
Requested \code{times} are aligned to that grid by nearest‐neighbor matching,
returning one survival estimate per requested time.
}
\examples{
\dontshow{if (requireNamespace("BART", quietly = TRUE)) withAutoprint(\{ # examplesIf}
mod_bart <- fit_bart(
  Surv(time, status) ~ age + karno + celltype,
  data = veteran
)
predict_bart(mod_bart, newdata = veteran[1:5, ], times = c(10, 30, 60))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link{fit_bart}}, \code{\link[BART]{surv.bart}}
}
