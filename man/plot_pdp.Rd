% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_pdp.R
\name{plot_pdp}
\alias{plot_pdp}
\title{Plot PDP/ICE Curves for Survival Models}
\usage{
plot_pdp(
  pdp_ice_output,
  feature,
  method = "pdp+ice",
  ids = NULL,
  which = c("per_time", "integrated"),
  alpha_ice = 0.2,
  smooth = FALSE
)
}
\arguments{
\item{pdp_ice_output}{The list returned by \code{compute_pdp()}.}

\item{feature}{Character scalar; the same feature analyzed in \code{compute_pdp()}.}

\item{method}{One of \code{"pdp"}, \code{"ice"}, or \code{"pdp+ice"} indicating which curves
to draw in per-time plots (ignored for integrated plots).}

\item{ids}{Optional vector of row ids (\code{.id}) to subset ICE curves for clarity
in per-time plots.}

\item{which}{One of \code{"per_time"} (facet by time) or \code{"integrated"} (plot the
time-integrated PDP if available).}

\item{alpha_ice}{Alpha transparency for ICE lines/boxes in per-time plots (default 0.2).}

\item{smooth}{Logical; if \code{TRUE} and the feature is numeric, apply a smooth
curve (\code{loess}) for integrated PDP; otherwise draw a line.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Plots partial dependence (PDP) and/or individual conditional expectation (ICE)
results returned by \code{\link{compute_pdp}} either per evaluation time or as
an integrated PDP over time.
}
\details{
\strong{Per-time:} For numeric features, draws ICE lines and PDP overlays per time.
For categorical features, shows ICE as boxplots per level and PDP as point summaries.
\strong{Integrated:} Plots the PDP integrated across time (if provided by
\code{compute_pdp()}); numeric features can be smoothed with \code{smooth=TRUE}.
}
\examples{
mod <- fit_coxph(Surv(time, status) ~ age + karno + trt, data = veteran)
pdp_age <- compute_pdp(
  model = mod,
  data = veteran,
  feature = "age",
  times = c(80, 160),
  method = "pdp+ice",
  grid.size = 8
)
plot_pdp(pdp_age, feature = "age", which = "per_time")
plot_pdp(pdp_age, feature = "age", which = "integrated", smooth = TRUE)

}
