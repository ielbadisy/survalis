% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/times-grid.R
\name{default_times}
\alias{default_times}
\title{Default evaluation-time grid}
\usage{
default_times(time, status = NULL, n = 50L, range = c(0.05, 0.95))
}
\arguments{
\item{time}{Numeric vector of observed follow-up times.}

\item{status}{Optional 0/1 event indicator aligned with \code{time}. When supplied,
the grid is built from event times only; otherwise all positive times are
used.}

\item{n}{Integer; maximum number of grid points (default \code{50}).}

\item{range}{Two-value probability vector giving the lower and upper quantiles
of the event-time distribution spanned by the grid (default
\code{c(0.05, 0.95)}), which trims unstable tails.}
}
\value{
A numeric vector, strictly increasing and positive, of length at most
\code{n}.
}
\description{
Builds the time grid used by the verb-level functions (\code{\link[=predict]{predict()}},
\code{\link[=evaluate]{evaluate()}}, \code{\link[=tune]{tune()}}, \code{\link[=compare]{compare()}}, \code{\link[=interpret]{interpret()}}) when the caller does not
pass \code{times} explicitly. The grid spans the observed event times so that
integrated metrics (IBS, IAE, ISE) and survival-curve summaries are computed
over the region where the data are informative.
}
\details{
The chosen grid is recorded on every result object (\code{res$times}) and printed,
so that runs remain reproducible and comparable even though the default is
data-dependent.
}
\examples{
default_times(veteran$time, veteran$status)
default_times(veteran$time, veteran$status, n = 10)
}
