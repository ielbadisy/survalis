% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_default_survlearners.R
\name{benchmark_default_survlearners}
\alias{benchmark_default_survlearners}
\title{Benchmark Multiple Survival Learners (Cross-Validation Wrapper)}
\usage{
benchmark_default_survlearners(
  formula,
  data,
  learners,
  times,
  metrics = c("cindex", "ibs"),
  folds = 5,
  seed = 123,
  ncores = 1,
  verbose = FALSE,
  suppress_errors = TRUE,
  ...
)
}
\arguments{
\item{formula}{A survival formula of the form \code{Surv(time, status) ~ x1 + x2 + ...}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{learners}{Character vector of learner ids (without prefixes), e.g.
\code{c("ranger","coxph","glmnet")}. For each id, \code{fit_<id>} and
\code{predict_<id>} must exist.}

\item{times}{Numeric vector of evaluation time points for survival predictions.}

\item{metrics}{Character vector of metrics to compute in CV (e.g., \code{"cindex"},
\code{"ibs"}, \code{"iae"}, \code{"ise"}). The metric semantics are those of
\code{cv_survlearner()}.}

\item{folds}{Integer number of CV folds (default \code{5}).}

\item{seed}{Integer random seed for fold generation.}

\item{ncores}{Integer; number of CPU cores passed to \code{cv_survlearner()}
for fold-level parallel mapping (default \code{1}).}

\item{verbose}{Logical; if \code{TRUE}, prints progress per learner.}

\item{suppress_errors}{Logical; if \code{TRUE} (default) errors from individual
learners are caught and reported via \code{warning()} and benchmarking continues.
If \code{FALSE}, the first error is thrown.}

\item{...}{Additional arguments forwarded to each learner's \code{fit_*()}.}
}
\value{
A data frame of CV results (as returned by \code{cv_survlearner()})
with an extra column \code{learner} identifying the source learner.
}
\description{
Runs \code{cv_survlearner()} for a set of learner names (e.g., \code{"ranger"},
\code{"coxph"}) by dynamically dispatching \code{fit_<learner>} and
\code{predict_<learner>} functions. Returns the row‑bound CV results across
all requested learners.
}
\details{
Learners are run independently using identical CV splits and scoring settings.
Any learner whose \code{fit_*()} or \code{predict_*()} function is missing will
be skipped with a warning. At least one learner must complete successfully or an
error is raised.
}
\examples{
\donttest{
res <- benchmark_default_survlearners(
  Surv(time, status) ~ age + karno + trt,
  data = veteran,
  learners = c("coxph", "rpart"),
  times = c(80, 160),
  metrics = c("cindex", "ibs"),
  folds = 2,
  seed = 1
)
head(res)
}

}
\seealso{
\code{\link[=cv_survlearner]{cv_survlearner()}}, \code{\link[=plot_benchmark]{plot_benchmark()}}, \code{\link[=summarise_benchmark]{summarise_benchmark()}}
}
