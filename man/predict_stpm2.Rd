% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_stpm2.R
\name{predict_stpm2}
\alias{predict_stpm2}
\title{Predict Survival Probabilities with an rstpm2 Model}
\usage{
predict_stpm2(object, newdata, times, ...)
}
\arguments{
\item{object}{A fitted \code{mlsurv_model} returned by \code{\link[=fit_stpm2]{fit_stpm2()}}.}

\item{newdata}{A \code{data.frame} of new observations for prediction. Must include
the same predictor variables used at training time.}

\item{times}{Numeric vector of evaluation time points (same scale as the
training survival time). Must be non‑negative and finite.}

\item{...}{Additional arguments forwarded to \code{predict()}.}
}
\value{
A base \code{data.frame} with one row per observation in \code{newdata} and
columns named \code{t={time}} (character), containing numeric survival
probabilities in [0, 1].
}
\description{
Generates survival probabilities at specified time points using a fitted
\pkg{rstpm2} model wrapped as an \code{mlsurv_model}.
}
\details{
Internally expands \code{newdata} over the requested \code{times} and calls
\code{predict(object$model, type = "surv", newtime = times)}; results are
reshaped to a wide format with one column per time point.
}
\examples{

mod_stpm2 <- fit_stpm2(Surv(time, status) ~ age + karno + celltype,
                       data = veteran, df = 4)
predict_stpm2(mod_stpm2, newdata = veteran[1:5, ], times = c(100, 200, 300))

}
\seealso{
\code{\link[=fit_stpm2]{fit_stpm2()}}
}
